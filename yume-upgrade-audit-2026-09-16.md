# Yume 升级与全面审查报告

日期：2026-09-16。范围：Yume 请求与图片链路、固定技能和工具调度、界面、游戏库读写融合、导出、发布边界及后续开发。§1–§9 保留当时的审查记录；当前 [AI 文档 v1.10](ai-integration-development.md)、[总开发文档 v0.10](docs/galgame-launcher-development.md) 的本地事实查询修复见 §10。§9 的对话式管家取代更早记录中的手动关联、默认关闭开关和独立封面入口。

## 1. 结论

本轮已把固定问答流程升级为可选择工具、接收结果、继续判断并结束的有限智能体。完成四种内嵌技能、五个工具、单次视觉分析、VNDB 检索、游戏资料审核/撤销和文档导出；微信透明剪贴板、请求期限、协议校验、写库冲突与导出错误边界已修复或补强。

当前适合开始功能试用；尚不能宣告真实模型语义准确率、所有供应商兼容或正式发行验收通过。在本轮覆盖的源码、模拟服务、隔离数据和桌面检查中，未发现尚未处理的可复现高危越权或数据丢失问题；这不等于证明不存在漏洞。

## 2. 实际交付能力

| 功能 | 当前行为 | 限制 |
| --- | --- | --- |
| 专用智能体 | 固定角色约束，模型判断意图和下一步，工具结果反馈后再判断 | 领域判断依赖模型；无关请求仍会消耗一次判断请求 |
| 技能 | role、vision、library、research 的 v1 资源嵌入程序集 | 开发者改源码后重编译；无用户技能/插件热加载 |
| 工具 | library_search、game_details、search_vndb、propose_metadata、create_document | 固定 JSON 指令，不是原生 function calling 或任意代码执行 |
| 预算 | 每任务最多 6 次模型、4 次工具、2 次 VNDB 搜索；重复请求提前停止 | 不自动重试；尚无 token/费用统计 |
| 识图 | PNG 优先，修复微信 DIB alpha，首个视觉请求完成范围判断与线索提取 | 后续用文字模型整理；未知作品仍可能无法识别 |
| 联网检索 | 用户启用后查 VNDB 官方作品 API，展示真实来源卡 | 非通用网页、攻略全文、反向搜图或联网保证 |
| 资料补全 | 显式关联游戏，生成默认不勾选的字段建议，确认后保存 | 不自动改封面/路径/统计；来源尚未逐字段绑定 |
| 撤销 | 撤销本次运行内最近一次 AI 保存，先检查冲突 | 不是跨重启或无限层级撤销 |
| 文档 | 草稿预览，用户选择位置导出 Markdown/HTML | HTML 为安全纯文本模板；无详情入口或 GamePages 管理 |
| 能力测试 | 独立合成数字图/工具 JSON 探测，按配置身份隔离 | 会话内诊断提示；未验证也可尝试发送，不是自动模型能力匹配 |

单次模型等待默认 60 秒、整任务默认 180 秒。原 AiTimeoutSeconds 保留用于服务/文字测试；启动不重写旧配置。流式正文逐步显示，停止/异常保留已收到内容。技能扩展沿用 .NET/WPF 和标准库，没有引入完整 DSH、Node.js、LangChain 或新第三方包；这是采用 ponytail 技能后对实现复杂度的控制。

## 3. 漏洞、Bug 与防护审查

| 项目 | 风险/发现 | 本轮处理与验证 |
| --- | --- | --- |
| 微信剪贴板 | PNG 正常但 WPF 位图 alpha 全零，预览和上传可能透明 | 三个粘贴入口共用 PNG 优先读取；DIB 兜底在缩放前修复，保留合法透明 PNG；本机实际粘贴成功 |
| 图像与资源 | 巨图、损坏输入、全透明图导致错误或资源占用 | 32 MiB/4000 万像素检查；缩放、4 MiB PNG 与缩略图上限；发送前拒绝无效内容 |
| 多阶段期限 | 原 30 秒整任务容易截断两个健康阶段 | 分离请求等待/任务期限；SSE 数据事件刷新等待但不刷新总期限；模拟慢阶段通过 |
| 重复上传 | 相关识图的两个请求曾重复携带同图 | 当前只有首次视觉请求带图，后续复用线索；线级请求检查通过 |
| 任意工具/循环 | 模型输出 shell、未知参数、重复指令或无限续轮 | 固定允许清单、参数校验、重复 JSON 键拒绝、次数/时间/响应上限；越权和重复回归通过 |
| 游戏库越权读取 | 模型可能索要任意游戏/本地路径 | 默认不提供全库；显式关联或摘要查询许可；限定 ID 和摘要字段，不暴露文件读取工具 |
| 草稿越权写入 | 未确认修改、旧草稿覆盖手动编辑、删除条目被复活 | 模型仅产草稿；默认不勾选；按 ID/元数据指纹/库状态校验，统一持久化入口 |
| 写入失败/运行统计 | 保存失败污染内存，AI 更新替换正在运行对象 | 先保存副本再更新原对象；隔离失败、重载、运行中刷新/保存、进程退出和撤销回归通过 |
| 配置与历史混用 | 改服务后旧对话或旧测试成功被带入新配置 | 配置身份隔离、清模型历史、取消旧探测；旧活动任务结果不进入新历史 |
| API 凭据与重定向 | 密钥泄漏到新站点、错误或日志 | DPAPI、来源变更清 Key、禁止重定向和 URI 凭据；日志仅分类/次数/耗时/异常类型 |
| 检索目标 | 模型构造任意 URL、伪造来源地址 | 只访问固定 VNDB API；来源 ID 严格校验后构造 URL，禁 cookie/重定向，不转发模型 Key |
| HTML 注入 | 资料中脚本/远程图片在导出时执行 | 全文 HtmlEncode + CSP；不把 Markdown 内的 HTML 当可执行内容；恶意文本回归通过 |
| 导出错误 | 用户输入非 .md/.html 扩展时 InvalidDataException 可能逃出 UI | ExportDocument 显式捕获该异常并给出可恢复提示；核心扩展限制检查通过 |
| 设置测试异常 | 读取 HTTP 响应的 IOException 捕获遗漏 | 服务/文字测试补齐 IO 捕获，错误安全反馈 |
| UI 与反馈 | 新控件挤占窄窗口、审核字段/来源夜间颜色遗漏、过时说明 | 压缩留白、保持输入和发送栏固定、审核区滚动；统一主题资源，修正功能提示 |
| 发布扩展入口 | 外部文件改变发行版角色/工具 | 资源和清单编译固定，无插件路径、环境变量或命令行覆盖入口；源码检查与资源回归通过 |

模型提示不能保证永不偏题或不受注入影响，代码强制保护的是工具、数据和写入权限。库简介、用户问题或查询词里的个人信息不会被自动识别脱敏；用户启用联网后模型生成的精简查询词会交给 VNDB。源资料和模型事实必须人工核对，不能把取得来源卡等同于逐字段已证实。

## 4. 与本地游戏库的融合程度

当前已形成“选中游戏 → 显式关联 → 生成建议 → 勾选保存 → 库刷新 → 可撤销”的闭环，属于可用的资料协作层。游戏启动和文件管理仍由原本地功能负责，没有交给模型自主执行。

| 场景 | 行为/证据 |
| --- | --- |
| 主库选择与 AI 关联 | 关联对象在页头可见，切换导航保留；已在真实窗口选中并关联樱之诗条目验证 |
| 按标题/标签找游戏 | 本地匹配后只返回最多 5 个简要结果，再按授权 ID 获取详情；多步模拟模型通过 |
| 上下文随资料变化 | 每次发送刷新关联摘要；元数据变化清历史，目标删除要求重新关联 |
| 重复保存 | 草稿完成后清除；即使旧草稿再次提交，版本检查也阻止覆盖 |
| 仅接受简介 | 只更新简介，保留引擎、路径、参数、收藏、封面、ID 和统计；保存后重载一致 |
| 写入失败 | 保留旧文件及活动对象；草稿可继续审核/重试 |
| 人工编辑/删除冲突 | 不覆盖更新资料，也不恢复已移除条目 |
| 游戏运行时保存 AI 资料 | 使用无害 TestGame 实际启动；刷新后保存仍保留对象，进程退出统计与新标题同时存在 |
| 统计更新后撤销 AI 修改 | 撤销元数据但保留新记录的游玩时长 |
| 无 AI / 不联网 | 原添加、扫描、编辑、收藏、刷新、启动与存储回归继续通过 |

尚缺的深度融合：游戏详情页补全快捷入口、资料页索引、已保存资料来源历史、封面候选审核、持久撤销、添加后主动启用的自动草稿。此次不以百分比概括完成度，以以上可验证场景划分边界。

## 5. 代码质量与维护

- Provider/SSE、图片输入、调度、VNDB、资料模型和导出职责已分开；复用已有 RelayCommand、OperationFeedback、JsonGameStore 与 MVVM，不增加空接口或通用插件宿主。
- 五个工具用显式清单和 switch 校验，规模仍可维护；后续工具明显增多时再抽取统一注册描述，避免清单、提示词和分支不一致。
- 使用取消令牌、有界响应、严格 JSON 类型/字段校验；字段保存保持统一入口与原对象身份。测试使用真实 WPF 绑定及回环分段 HTTP，不仅验证字符串拼接。
- 能力结果仅内存缓存；凭据索引为哈希。密钥仍以必要的明文驻留请求内存，DPAPI 不意味着进程内永不出现明文。
- 当前 API Key 的保护字符串重新加密可能让一次设置保存触发保守的历史清空；不会泄漏数据，但后续可用稳定的服务配置身份避免无关清空。
- 角色/工具原文仅为可维护的开发资源，固定在发布包中不等于能防止拥有本机控制权的人反编译或修改二进制。

## 6. 验证记录

统一门禁：Release 编译零警告/零错误、SettingsChecks 全通过、ReliabilityChecks 240 项通过、无 WPF 绑定错误、依赖框架发布/图标/版本检查通过。原默认基线为 202 项；本轮新增 38 项，包含智能体及游戏库交叉场景。测试运行不等于真实模型语义验收。

- 最终完整门禁日志：`.runtime/checks/check-20260916-182623.log`（含导出错误捕获和设置提示收尾修正）。
- 最终可靠性证据：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/b90fb3663924435eb0f2856b8de43564/`。
- 最终设置/导航证据：`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/6e4e5e912679460bb8ab13764ce4421f/`。
- 审核夜间渲染补验：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/71aef8cb1ba14975ae92bb8ac11440bb/yume-metadata-review-night.png`，补验仍为 240 项全通过。修正的是测试宿主背景，避免把脱离 Window 的白底渲染误当夜间结果。
- 桌面实测：主库选择、AI 关联反馈、微信粘贴缩略图 895×703/58.2KB、移除待发送附件、设置分类/滚动和四种检测入口。未点击实际模型测试/发送按钮。
- 公开网络实测：VNDB 官方接口查询公开作品 CLANNAD，返回 v4 / CLANNAD / 2004-04-28。未向其发送用户库或模型 Key。参考：[官方 API 文档](https://api.vndb.org/kana)。
- 发布产物：`.runtime/publish/`，依赖 .NET 8 Desktop Runtime；未完成无 SDK 干净 Windows、自包含安装包、签名或升级覆盖验收。
- 已直接检查发布 DLL 的 manifest，包含 library/research/role/vision 四个技能资源；正式 Release 构建零警告/错误，新进程 PID 23732 正常响应，程序已留待试用。

真实数据前后 SHA-256 保持一致：library.json 为 `3A5EF38B6A22F699299FD931B6EAB300E398CA898429CECAC5084C608F61ADD6`，settings.json 为 `891DA293D267EA2EF46A7E188923AFCAB11097BC69A318A62FCA7824B4992066`。未修改用户实际游戏条目、配置或系统剪贴板；测试附件只在内存中准备并移除。既有未提交改动保留，本轮不提交或推送 GitHub。

## 7. 后续优先级与验收标准

| 优先级 | 方向 | 需要的验证 |
| --- | --- | --- |
| P0 · 正式发行前 | 真实服务/视觉/语义评估 | 至少 40 条相关、无关、简称、追问、混合意图、注入和剧透样本；记录模型版本/误放行/误拒绝、延迟与用量；当前未执行 |
| P1 | 每个字段的来源与证据 | 建议字段绑定 source ID、摘录、日期与作品版本；无证据标未知，冲突保留差异，人工确认始终保留 |
| P1 | 资料页深入游戏库 | GamePages 按 ID 管理、详情打开/重新生成、前一版本恢复、游戏移除与资料页生命周期 |
| P1 | 真实供应商能力矩阵 | 基础视觉/JSON 探测、参数支持、错误分类和按已验证能力降级；不得自动切换厂商或凭据 |
| P2 | 更广的检索覆盖 | 先增加可靠中文/日文作品源；若抓网页需独立实现 SSRF、DNS、重定向、大小和来源校验 |
| P2 | 会话体验 | Markdown 安全渲染、可点击来源、历史持久化/清理、资料区定位、回答长度及剧透偏好 |
| P2 | 检索与执行效率 | usage 统计、证据去重/缓存、按任务缩小提示上下文；以实测改善延迟，不无限压低超时 |
| 发行门槛 | 安装与桌面兼容 | 干净 Windows 无 SDK、100/125/150% DPI、物理中文输入法、读屏器、自包含包与升级保留数据 |

下一步宜先对用户实际供应商做小规模明确授权的模型/视觉验证，再扩展证据和详情页；无需以导入通用 Harness 或开放用户插件作为前置条件。

## 8. 封面协作补全与复验（2026-09-16 晚）

用户实测失败时，v1.7 尚无封面工具，VNDB 只查询文字字段；一次最新请求停在模型判断阶段，1次模型/0次工具后60秒超时。日志不能区分供应商排队、网络或推理耗时，不把新增工具说成修复供应商超时。

现新增第六个工具 find_cover、library@2/research@2，以及无需模型 Key 的直接查询入口。查询 → 候选 → 缩略预览 → 明确确认 → 下载/保存 → 可撤销已实现；旧结论中“封面候选审核未实现”由本节取代。保存和撤销不向模型暴露为自动操作，域外、缺权限、无关联目标或未知 URL 参数仍受代码约束；重复无执行动作的分类提前停止。

| 检查面 | 本轮结论 |
| --- | --- |
| 来源与下载 | 固定 VNDB API/CDN 路径，禁止重定向/凭据转发；普通封面评分过滤、缺图和重复处理；MIME/魔数/有无长度头的字节限制与图片解码校验 |
| 写库事务 | 搜索/预览不落盘；确认后以唯一文件名保存干净 PNG，库存储成功后更新原对象；失败清理本次未提交图，原封面不变 |
| 冲突和撤销 | 下载前后检查完整资料/封面版本；后续人工修改和删除阻止旧草稿；保存/撤销失败可重试，已提交图片保留以兼容库备份 |
| 运行融合 | 无害 TestGame 运行中换封面保持对象身份，退出后封面和统计均持久；撤销保留最终游玩时长 |
| UI | 候选、作品来源、新旧对照、确认/放弃/撤销与状态反馈；日夜及680×540内容区渲染和绑定通过，确认按钮可滚动访问，预览后定位确认区 |
| 真实网络 | 公开查询“樱之诗”命中 v562，缩略图和原图成功；原图801×560、重编码860169字节，无模型请求、未写用户库 |

最终 `scripts/check.ps1`：Release 零警告/零错误，SettingsChecks 全通过，ReliabilityChecks **297项**通过，无WPF绑定错误，发布/图标/版本检查通过。测试使用虚构Key、回环模型、合成图片、隔离库和无害进程；新增自然语言封面工具完整循环回归。没有调用用户真实付费模型，不将模拟分类结果等同于供应商实际工具遵循率。

- 门禁日志：`.runtime/checks/check-20260916-193538.log`。
- 可靠性/渲染证据：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/b3b2a67d3efc436080e7e6c840e9f437/`。
- 设置证据：`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/6e52f4484926403c916985c1fc514ec9/`。
- 公开来源证据：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/d5ee4ba493ae43178890f9960318e03b/public-cover-source.json`；可显式运行 ReliabilityChecks 的 `--cover-source-check` 单独复验，日常门禁不会访问外网。
- 正式 Release 构建后启动 PID14344，窗口及新封面入口可见，已恢复“樱之诗V1.2”关联。用户同时操作窗口时停止UI输入；未发送模型请求或更改联网许可。重启前的未发送问题保留于 `.runtime/restart-ai-draft-20260916.txt`，未替用户提交。
- 本轮前后真实 library.json SHA-256：`3A5EF38B6A22F699299FD931B6EAB300E398CA898429CECAC5084C608F61ADD6`；settings.json：`C56133C26E883FDD6B0431A4A85EA0FA1BE54D6E1EC5EF8D8AC0D7785410535D`。设置哈希与§6旧轮次不同，但本轮开始与重启后一致，未恢复/覆盖用户后来的配置。

后续：真实模型工具调度成功率与延迟仍需评估；VNDB 覆盖之外的作品、中文别名、智能裁切、持久撤销、来源历史和缓存清理未实现。严格固定源不等于通用网页抓取的 SSRF 防护方案，增加任意站点时需重新设计。当前缓存保留策略会随多次成功更换增加磁盘占用，后续清理需同时考虑当前库和备份引用。

## 9. 对话式游戏库管家（2026-09-16）

用户指出旧版需手动关联、勾选联网及操作独立封面面板，且组合请求可能只完成封面。此次将控制流程收回工具链：role@2、library@3、research@3、vision@1，共八个固定工具；不引入新运行时。新增 prepare_game_update 一次取得资料/封面，scan_games 复用已有本地识别和入库。顶部只留新对话，图片只留选择按钮与 Ctrl+V；审核和入库控件按需出现。

| 检查面 | 结果 |
| --- | --- |
| 目标定位 | 标题归一化提示、真实 ID 查询、主界面选中与对话上下文区分；点名覆盖其他选中项，无预选时也能查找。歧义交给模型澄清，伪造 ID 被程序拒绝 |
| 复合资料 | 单来源自动准备字段/缩略预览，多来源选择对应版本；只准备请求字段，非法来源不能进入草稿。简介为来源原文，未确认引擎/类型保持旧值 |
| 写入与撤销 | 资料/封面同一次原子库提交；失败完整回滚并清理本次未提交图片；成功更新原 Game，整次撤销保留新统计，已删除/被编辑目标不被覆盖 |
| 日期与类型 | 新增 ReleaseDate / GameType，旧库兼容，人工编辑和AI可维护；校验真实日历日期/年份一致性，游戏类型纳入主库搜索 |
| 路径入库 | 只接受用户当前消息给出的本机文件夹/EXE；禁止扩大为父目录、虚构路径、UNC；扫描不执行文件，选中候选后复用原去重、失败反馈和持久保存 |
| 失败体验 | 缩略图超时仍保留文字资料与成功任务，可只保存文字或重试图像；停止、重复提交、候选保存失败可恢复 |
| 布局与融合 | 日夜主题、680×540窄内容区、确认按钮可达、固定输入区、单个选图按钮、无旧关联/许可/独立封面控件；运行进程退出后资料/封面/统计均保留 |

验证证据：

- 完整门禁 `.runtime/checks/check-20260916-212159.log`：Release零警告/错误、设置/导航/反馈全通过、可靠性329项通过、无WPF绑定错误，依赖框架发布/图标/版本检查通过。
- 可靠性及渲染：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/99b2d2dec4f549c9a31e4d3035e5c16c/`。检查组合修改、缩略图超时、真实扫描、导入保存失败重试及启动退出统计，不写真实库。
- 设置证据：`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/82c00a03043d4a52ba0f546d6bdcd775/`。
- 新复合 VNDB 查询真实联网复验：公开作品“樱之诗”返回 v562、简介、2015-10-23和封面；缩略/原图正常，801×560、优化860169字节。证据 `.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/02ad13fdfb2a415dbdc99298707ceadf/public-cover-source.json`。未调用用户模型或发送用户库。
- 正式 Release单独重建、正常关闭PID22620后启动最终PID1264，无强制结束。桌面查看原3条游戏、进入简化AI页，窗口响应正常，只有一个选择图片按钮，未向真实模型发送消息。
- 本轮真实库/配置哈希前后一致：library.json `3A5EF38B6A22F699299FD931B6EAB300E398CA898429CECAC5084C608F61ADD6`；settings.json `BA9FC41D0A82B7F45B4055687908D10EDF51C6EA79D7C4C6E40BF5C6CBF4DDC5`。用户后来修改的配置按本轮基线保留。

仍待验证/开发：模拟模型指令不证明真实供应商的意图理解或工具遵循率，60秒等待原因未查明；VNDB不是通用网络检索，简介可能英文、引擎/游戏类型资料可能缺失。模型可提出错误目标或不实建议，最终目标/来源/新旧值审核继续保留。后续优先真实模型场景评估和中文/日文可靠来源，再推进逐字段证据、持久历史与撤销、资料页索引；资源包下载后置。旧 ViewModel 封面/关联方法保留兼容既有测试，已无产品入口，后续可独立清理。

## 10. 本地数量/列表无依据回答修复（2026-09-16）

用户原问题“我现在的库里有几个游戏”，真实库3条，旧回答却列出5个不存在的作品，状态为2次模型/0次工具/38.9秒。已有本地读写回调及授权并未缺失：根因是模型能够跳过查询直接回答、正式回答提示丢失库总数，以及搜索最多5候选没有提供匹配总数。日志显示路由约24.5秒、等待正文约12.6秒、正文约1.8秒，无法进一步判断供应商内部耗时原因。

修复使用现有.NET库和调度器：library@4、第九工具library_query；明确整句数量/列表请求直接读取本轮快照并由程序回答，0模型/无需Key。其他统计通过模型给出受限count/list、筛选和offset参数，程序输出真实数量/列表后结束；每页20项。library_search返回totalCount/matchedCount/returnedCount/hasMore；两阶段系统提示都带本轮available/totalCount。answer缺少general声明或声明本地依据时要求纠正，连续不遵守则停止。库损坏、不可读或未保存状态与真正空库区分，不猜测为0。

验证：完整门禁 `.runtime/checks/check-20260916-220540.log` 通过，Release零警告/错误、设置/反馈/导航检查全过、可靠性 **345项**、无WPF绑定错误、依赖框架发布及资源检查通过。本轮新增16项回归覆盖原问句绕过假5条回答、同会话3→7→0快照刷新、无Key、库不可用、混合/筛选不误走快捷路径、27条库筛选/分页、跳过工具一次纠正或停止、两阶段总数和禁止模型传入假名单；原问题本地模拟实测2毫秒/0模型请求。

- 可靠性证据：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/5f80dc1a5f2d4cadae4534878ddd7847/`。
- 设置证据：`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/797bd2e6b6b04c4e918af04674cbef08/`。
- 测试使用隔离数据及回环模拟服务，没有调用用户真实模型或改写用户库。

正式运行：另行构建 `src/YumeShelf/bin/Release/net8.0-windows/`，零警告/错误。旧实例PID1264确认无未发送输入和活动任务；窗口自动化两次返回“failed to activate captured window”，随后通过进程正常关闭请求退出（CloseMainWindow=true，未强制结束）。新版PID26004于22:16:42启动，主窗口正常响应，无障碍控件树显示原3条游戏。当前桌面截图被其他正在使用的应用占据，因此停止后续UI输入，没有声称在真实窗口发送并验证原问题；2毫秒/0请求结论来自上述隔离回归。

重启前后真实数据SHA-256一致：library.json `3A5EF38B6A22F699299FD931B6EAB300E398CA898429CECAC5084C608F61ADD6`；settings.json `BA9FC41D0A82B7F45B4055687908D10EDF51C6EA79D7C4C6E40BF5C6CBF4DDC5`。未改用户配置、库记录或系统剪贴板，未提交或推送仓库。

保障边界：直接查询和library_query结果由代码生成；复杂语义仍由模型判断，错标general不能靠自报协议完全排除。game_details等工具后的知识性解释仍由模型生成，不能据此宣称所有卡片字段和混合任务已消除幻觉。后续真实供应商验收要加入绕过工具、模糊表述、多轮变库和组合任务成功率/耗时；不以缩短超时或额外引入Harness代替证据链约束。
