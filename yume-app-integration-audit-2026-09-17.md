# Yume 与 YumeShelf 应用契合度审查

初审日期：2026-09-17。初审基线：当时工作区，包括既有未提交代码；AI 文档 v1.10。以下初审章节保留原问题与证据，不代表修复后的状态。用户随后授权修补六项缺陷；当前 AI 文档 v1.11，修复与复审结论见文末“修复与复审”。初审本身没有修改产品代码或重启应用。

## 结论

Yume 的界面和游戏库读写已较好融入应用，具备真实可用的资料审核、封面更换、撤销及扫描入库链路。但对话任务的连续性、事实依据和完成判定仍不够稳，当前适合受监督的单项任务，尚不宜视为成熟的自主游戏库管家。

不能用“有没有本地库权限”解释目前的问题：权限与业务回调已经存在。主要问题是模型指令、工具结果、待确认草稿与应用状态之间的约定不够一致。上一轮修复保证了明确数量/列表快捷查询及 library_query 的真实输出；不代表任意自然语言都已受到同样保障。

| 维度 | 当前判断 | 依据及边界 |
| --- | --- | --- |
| 界面与导航 | 较好 | AI/设置均嵌入主窗口，主题、反馈、固定输入区与导航共用；切页保留会话，日夜与窄窗口回归通过 |
| 游戏库读写 | 较好 | 从同一 Games 集合生成摘要，资料与封面复用持久化、冲突检查、去重、撤销及原活动对象；运行统计兼容 |
| 对话意图与事实 | 部分契合 | 明确数量/列表可完全本地回答，复杂问法仍依赖模型的自报分类；存在误放行与误拦截 |
| 多轮任务与确认 | 不足 | 待确认结果挂在当前 ViewModel 单份属性上，普通追问会覆盖；没有逐项任务完成状态 |
| 对应用状态的理解 | 有限 | 能读卡片基本字段，但摘要/查询没有收藏、最近游玩、时长和启动可用性等字段；无法可靠回答这些管理问题 |
| 检索与资料页 | 部分契合 | 当前只有 VNDB 官方作品源；文档是手动导出的 Markdown/转义HTML草稿，没有与游戏ID关联的资料页索引 |
| 设置与模型能力 | 基础可用 | 配置、独立视觉/JSON诊断、超时与取消可用；诊断结果尚不驱动自动能力选择，真实供应商语义成功率未验收 |

## 可复现问题（按修复优先级）

### F1 · P1：模型自报 general 仍可绕过本地事实查询

位置：[YumeAgent.cs](src/YumeShelf/Application/AI/YumeAgent.cs#L188)，另见 [AiLibraryQuery.cs](src/YumeShelf/Application/AI/AiLibraryQuery.cs#L12)。

补充诊断使用3条虚构库记录，问题为“帮我盘点一下我收录了哪些作品”。模拟路由返回 answer_basis=general，后续正文故意返回“5个游戏，包括不存在的测试作品”。现有程序接受该回答，状态为工具0次。当前校验只拦截非general声明，没有根据证据检查正文中的本地事实。

影响：换一种未命中离线快捷路径的问法，之前的编造数量/名单问题仍可能发生。该测试证明错误模型响应能被放行，不代表真实供应商每次都会如此，也不能据此估算出现概率。

建议：本地读任务使用实际读取结果和受控呈现；调度结果关联本轮操作、目标与证据，不能把模型的general声明作为充分依据。保留正常作品知识回答，不通过不断增加关键词黑名单解决任意语义问题。

验收：明确/模糊问法、多轮指代及模型错误声明的用例中，无证据本地名单不能被当成已查库结果呈现；数量、名单和来源一致。

### F6 · P1：含空格路径的前缀可被误认为完整授权路径

位置：[AiLibraryTasks.cs](src/YumeShelf/Application/AI/AiLibraryTasks.cs#L38)。

在隔离目录中同时创建 `scan target` 和 `scan target extra`，用户明确提供带引号的后一个目录。模型若返回前一个目录，ValidateScanPath 仍会接受：它找到字符串前缀后，把紧随其后的空格当作路径结束，而未识别引号内完整路径。

影响：可能扫描用户没有指定的另一个目录，超出本轮路径范围。此次只调用路径校验，没有扫描用户文件，也不意味着会执行游戏或自动写入库。

建议：先从用户消息提取完整路径，优先支持带引号路径；规范化后与候选精确比较。无法确认边界时澄清，不接受任意前缀。保留现有本机路径、存在性和UNC限制。

验收：带空格/标点、带引号、多路径、正反斜杠及同名前缀目录，接受的目标必须是用户实际提供的完整路径。

### F2 · P2：普通追问会静默清掉尚未确认的修改

位置：[AiAssistantViewModel.cs](src/YumeShelf/Presentation/AiAssistantViewModel.cs#L291)。

先生成简介修改草稿并勾选字段，再问“我现在的库里有几个游戏”。新回答不携带草稿，SendAsync 仍无条件执行 Draft/Document/CoverDraft/Research/ScanDraft 赋值，之前的修改卡消失。聊天中“已生成资料建议”的消息还在，但确认入口和勾选状态已丢失。

影响：用户在确认前询问背景信息，会意外丢掉已准备的工作；同一赋值逻辑也覆盖资料文档和扫描候选。已持久化游戏数据没有被删除。

建议：让待确认任务独立于普通消息结果。只有用户放弃、提交成功，或明确替换同一任务时才清除；切换任务时保留目标、来源及勾选状态。

验收：修改/封面/入库候选生成后，进行普通追问、切页、取消请求，再返回仍可核对和确认；放弃操作才清除对应任务。

### F3 · P2：已经读取真实详情，仍可能无法给出本地回答

位置：[YumeAgent.cs](src/YumeShelf/Application/AI/YumeAgent.cs#L156)、[回答分支](src/YumeShelf/Application/AI/YumeAgent.cs#L188)。

问题“把审查游戏1在库里存的简介告诉我”：先成功执行game_details，再模拟模型如实声明answer_basis=library。程序仍要求改用工具，第二次相同声明后报“没有提供可验证的本地查询指令”。诊断共3次模型请求，最后没有输出已取得的真实简介。

影响：协议缺少“已用本地证据回答”的合法出口。提示一方面要求其他字段先读game_details，另一方面又只允许非本地事实使用general；守规矩的模型也可能陷入矛盾。

建议：为已验证详情提供明确的完成方式，例如直接格式化请求字段，或允许绑定已取得工具证据的回答。与F1一起修复，避免在误放行与误拒绝之间摇摆。

验收：读取真实简介/日期/引擎后可直接答复；未读取/错误ID/过期目标仍不能冒充已验证结果。

### F4 · P2：组合任务做完第一步就可能被标成全部完成

位置：[YumeAgent.cs](src/YumeShelf/Application/AI/YumeAgent.cs#L104)、[AiAssistantViewModel.cs](src/YumeShelf/Presentation/AiAssistantViewModel.cs#L303)。

问题“列出库里的游戏，并把审查游戏1的简介改成新版简介”：模拟模型先正确调用library_query。该工具立即return，之后的修改没有机会继续调度，页面清空问题并显示“完成”。没有生成修改卡，也没有说明第二项尚未处理。

影响：当前任务链更适合单个结果。prepare_game_update已支持同一作品的多字段/封面复合更新，但不能据此认定任意跨工具组合任务均可完成。

建议：最小化记录本轮请求的子任务及结果；只完成部分时保留剩余动作并如实显示状态。读结果保持程序生成，续步遵守已有预算；写操作仍等待确认，不借此自动写库。

验收：列库+修改、扫描+资料补全等组合任务不得漏项报完成；无法继续时明确剩余事项，不要求用户从头重复请求。

### F5 · P2：正常的“清空可选字段”操作无法完成

位置：[YumeAgent.cs](src/YumeShelf/Application/AI/YumeAgent.cs#L171)、[AiGameContext.cs](src/YumeShelf/Application/AI/AiGameContext.cs#L43)。

问题“清空审查游戏1的简介”：模拟propose_metadata给出Description空字符串，先被Text解析为缺少必要信息；字段校验也统一禁止空白值。不能形成可确认草稿。标签等可选字段存在同类限制。

影响：Yume目前能改成非空的新值，但不能覆盖普通编辑器常见的清空操作，作为“基本信息管家”仍有断点。

建议：区分字段缺失与明确清空，对可选字段允许经过审核的空值；标题等必填字段仍禁止清空。撤销应恢复原值。

验收：清空简介/标签/可选日期均能预览、确认、保存和撤销；缺参数、空标题依旧拒绝。

## 已做得较完整的部分

- MainWindowViewModel提供真实游戏摘要、选中项、统一提交/撤销、扫描和导入回调，无需用户手动关联。主界面选中项与对话目标有区分，明确点名可覆盖当前选中项。
- ApplyAiUpdate先持久化副本，再更新原Game对象；资料与封面统一保存，失败不留下半更新。指纹冲突、被删除目标、只读及未保存状态均检查；不会把已删除条目复活。
- 运行中更新保留活动Game对象，游戏退出统计与资料/封面修改兼容；撤销保留新增游玩时间。扫描入库复用现有识别、去重和持久化。
- 选择/粘贴图片不立即上传；视觉消息仅首轮带图，后续使用文字线索。普通封面下载走固定VNDB源，模型凭据不转发到公共图片服务。
- 取消、重试、流式内容保留、切页状态、主题同步与反馈已有回归，不需要再为这些基础衔接更换智能体框架。

## 能力缺口与代码质量

以下属于目前未实现/有限支持的能力，与上面的已复现缺陷分开对待：

1. **库管理上下文不完整。** AiGameSummary只含卡片文字，library_query只支持单个title/engine/type/tag/year条件。应用已有收藏、最近游玩、运行/缺失状态与时长，Yume不能可靠完成“列出收藏但未玩的游戏”“查启动文件失效项目”等。可增本地受控查询，不必将路径或全量记录上传模型。
2. **检索范围和产品目标有差距。** 只有VNDB作品API，缺少中文/日文多源、攻略全文或通用网页检索；引擎、类型和标签不会被VNDB资料自动补齐，简介保留来源原文。这些能力不能依靠更换模型或角色提示补出来。
3. **资料页没有与游戏绑定。** create_document返回草稿，用户选路径导出；没有以game ID管理的文档索引、详情入口或版本恢复。HTML目前安全转义后放进pre，尚非排版完整的资料网页。
4. **会话只存在内存。** 模型短历史会裁剪，重启不保留会话及单次撤销记录；与长期管家的预期有差距，但不是本轮新增回归。
5. **旧交互流程残留。** AiAssistantViewModel.Covers保留独立封面/关联命令，产品XAML已不绑定这些旧入口；和统一修改流程并存，增加后续维护及错误文案回归风险。宜在确认无兼容用途后渐进清理，无需整体重写MVVM或引入DSH/LangChain运行时。
6. **能力测试不是自动调度。** 视觉/JSON探测结果只用于设置提示，当前按用户配置和有无图片选模型。报告“测试成功”不能当作所有业务任务成功率的证明。

修复优先级：先F1/F6和F3的事实/权限约定，再F2/F4的任务保留与完成判定，随后F5及库管理查询覆盖。更多资料源、资料页索引和持久会话可在这些链路稳定后逐项推进。

## 验证与证据

- 当日运行 `scripts/check.ps1`：Release零警告/错误，SettingsChecks全部通过，ReliabilityChecks **345项**全部通过，无WPF绑定错误，依赖框架发布与资源校验通过。
- 门禁日志：`.runtime/checks/check-20260917-180343.log`。
- 可靠性证据：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/286ecacbbf3f445d8152f7aaf6de13f8/`。
- 设置/渲染证据：`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/708e9efadc3e4168a0970268dbf5b567/`。
- 补充诊断：`.runtime/yume-fit-audit-20260917/FitAudit.csproj`，复用现有测试的回环模拟服务与等待机制；引用当日构建DLL，不修改生产或正式测试源码。运行命令：`dotnet run --project .runtime/yume-fit-audit-20260917/FitAudit.csproj -c Release`。
- 诊断结果：`.runtime/yume-fit-audit-20260917/bin/Release/net8.0-windows/evidence/findings.json`。其中REPRO断言通过表示确认缺陷存在，**不是缺陷已修复**；共六项问题和一项前置状态断言。
- 模拟请求用于确定代码行为，不测量真实供应商语义准确率或延迟。未使用用户API、读取/修改真实游戏记录、主动访问外部检索源；未进行本轮真实桌面交互、跨DPI或安装环境验收。

原有345项全过与六项诊断复现并不矛盾：既有测试覆盖了已实现路径，尚未覆盖上述错误路由、待确认任务追问、含空格路径及组合动作。这些诊断应在修复时转为期望正确行为的正式回归。

## 修复与复审（2026-09-17）

六项已复现缺陷均已修补，并转为期望正确行为的正式回归。此次修改复用现有C#工具、ViewModel、存储和回环测试服务，没有引入智能体框架或额外运行时依赖。新门禁为382项可靠性检查全部通过，比初审增加37项。此处“通过”指确定的代码行为和模拟协议场景，不代表真实模型对所有自然语言的准确率已验收。

| 原问题 | 修复结果 | 复审依据 |
| --- | --- | --- |
| F1 general绕过事实查询 | 生产界面增加独立任务核对，未取得所需本地结果不能仅凭general声明生成答案；一次纠正后仍跳过则停止 | AiFitChecks模拟general错标→真实列表，以及连续错标→拒绝正文；核对消息不包含首路由自报依据 |
| F6 路径前缀越界 | 提取完整引号/裸路径后规范化精确比较，再检查存在性 | 英文双/单引号、反引号、中文双/单引号及裸路径含空格；同名前缀拒绝，斜杠/大小写/尾分隔符等价接受，多路径与嵌套盘符反例 |
| F2 普通追问丢草稿 | 仅更新本轮确有的新结果，同类冲突保留旧结果；保存/撤销不清理其他类型 | 勾选简介后追问数量，草稿引用/勾选、文档/依据、扫描引用/选择保留；旧目标被手改后仍保留审核卡，保存时拒绝冲突 |
| F3 真实详情误拦 | game_details返回可用于library完成分支，由程序格式化真实字段 | 返回库中实际简介，不请求后续模拟的伪造正文；详情+修改组合同时保留原详情与新草稿 |
| F4 首步即完成 | 记录任务类型及已完成结果，读工具后可继续，异常/取消时明确部分完成 | 列库+修改同时返回列表与审核卡；后续HTTP失败保留列表，后续取消保留草稿与原问题；正常知识回答仍真实SSE |
| F5 无法清空字段 | 可选字段接受明确空字符串，审核显示“（清空此项）”；标题/未知字段仍拒绝 | 隔离库清空简介、标签、引擎、类型、日期及年份，确认后重新加载验证空值，撤销恢复所有原值 |

核心实现：`Application/AI/YumeAgent.cs`（任务核对、真实详情、续步、部分完成）、`AiLibraryTasks.cs`（路径）、`AiGameContext.cs`（清空字段）；`Presentation/AiAssistantViewModel.cs`及`.Library.cs`负责保留/消费对应审核结果。`AiAssistantPage.xaml`显示清空状态并提供放弃资料草稿入口。内嵌library技能升为v5，仍4技能/9工具。正式测试位于`tests/YumeShelf.ReliabilityChecks/AiFitChecks.cs`；既有库、封面和调度测试显式增加核对响应，没有过滤请求或伪造调用次数。

复审同时确认：写入仍经用户选取和确认，先落盘再更新活动对象；库冲突/只读、失败回滚、去重、运行游戏退出统计、封面下载边界、凭据不外传、图片单次上传、工具预算、无WPF绑定错误等既有门禁继续通过。新增事实保护没有使常规知识问答退回整段等待输出。

### 保留的能力边界与后续事项

1. 独立核对仍是模型语义判断，不是绝对安全证明。首路由与核对同时误判、首轮误拒绝/误澄清、目标/字段错误、知识回答混入本地断言等仍需真实样本验证。实际工具参数、路径、ID和写入确认不交给模型决定。
2. 非快捷执行任务比原流程多一次短模型请求（temperature=0，输出上限300 tokens），计入原6次模型/4次工具预算。明确库数量/列表仍0模型；通常单工具任务2次、普通知识3次。未用真实服务测量新增延迟或准确率收益，不能宣称复杂任务更快。
3. 完成集合按任务类型记录，不追踪任意多个同类型目标；每轮仅一份修改、一份文档、一份扫描候选。跨游戏批量同类任务须分步，不应被当作任意批量工作流。已有同类草稿在收到新结果时阻止覆盖，可能已经产生此次模型请求；当前优先防止丢失选择，没有新建任务队列UI。
4. 未保存审核卡/文档/扫描结果、会话和撤销仍在内存；显式新对话会清除待确认内容，重启仅保留已保存数据。旧独立封面兼容命令仍在源码，产品页面没有其入口；其清理列为后续维护。
5. 初审列出的收藏/游玩/失效启动项查询、多源资料、游戏资料页索引、持久会话及能力驱动模型自动适配仍为后续需求，本次没有把这些尚未实现的能力标成完成。

### 最终验证记录

- 完整门禁：`.runtime/checks/check-20260917-193601.log`；Release零警告/错误，设置检查全过，可靠性**382项**全过，无WPF绑定错误，依赖框架发布、资源、版本与图标检查通过。
- 可靠性与渲染证据：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/2ec360c176684712a861cd737d342703/`；设置证据：`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/6c2aa8485779443a998b2efdfb5d8682/`。已人工查看小窗口确认区与夜间修改卡渲染，输入与确认按钮可见。
- 另行构建实际运行的`src/YumeShelf/bin/Release/net8.0-windows/YumeShelf.exe`，零警告/错误。启动前进程与桌面清单均未发现旧实例，故没有关闭或强杀进程；启动新版PID **22172**，窗口正常响应。
- 正式窗口通过computer-use进入AI页，发送无附件的“我现在的库里有几个游戏”：返回**3个游戏**，页面状态为**模型0次、工具1次、42毫秒**。UIA set_value接口报缓存属性错误，改用已观察的输入区点击和键盘输入后完成；没有通过修改配置绕过验证。
- 回归使用隔离库、合成图片、虚构Key和回环HTTP，未消耗用户真实模型额度。正式窗口仅执行确定性的本地计数，未修改用户库或设置。启动前后SHA-256一致：library.json=`3A5EF38B6A22F699299FD931B6EAB300E398CA898429CECAC5084C608F61ADD6`，settings.json=`BA9FC41D0A82B7F45B4055687908D10EDF51C6EA79D7C4C6E40BF5C6CBF4DDC5`。
- 本轮代码复审未发现六项修补范围内的新阻塞性回归；真实供应商语义、任意组合任务、跨DPI及干净安装环境未因此获得验收。未提交或推送GitHub，保留原有工作区修改。
