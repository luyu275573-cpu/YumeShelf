# 项目协作约定

- 使用中文沟通，保留工作区中与当前任务无关的修改。
- 每次需求或实现发生调整，同步相关开发文档和 `docs/development-log.md`；AI 变更同步根目录 `ai-integration-development.md`。
- 文档面向人阅读并可复盘：专项文档写清当前行为、设计理由和已知边界；开发日志按日期追加需求来源、修改原因、实现摘要、验证结果/证据和遗留事项，并链接相关文档。区分计划、已实现与已验证，不覆盖历史结论，复审以追加记录说明变化；README 维护文档入口。
- 用户明确要求：每次应用修改完成并验证后，都要重新构建并重启应用。优先正常关闭旧实例，保留用户数据和未保存内容；验证新实例可响应后再交付。
- 日常门禁为 `scripts/check.ps1`（Release 构建、设置检查、可靠性回归及发布检查）。门禁输出在 `.runtime/`；重启需另行构建实际运行的 `src/YumeShelf/bin/Release/net8.0-windows/YumeShelf.exe`。
- 回归使用隔离游戏库、合成图片和虚构 API Key，不为测试改写用户真实游戏库或消耗真实模型额度。
