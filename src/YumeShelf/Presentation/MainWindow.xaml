<Window x:Class="YumeShelf.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:YumeShelf.Presentation"
        xmlns:common="clr-namespace:YumeShelf.Common"
        Title="YumeShelf"
        Width="1200"
        Height="760"
        MinWidth="900"
        MinHeight="620"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackground}">
    <Window.Resources>
        <common:CoverImageConverter x:Key="CoverImageConverter"/>
        <Style x:Key="ShellNavButton" TargetType="Button" BasedOn="{StaticResource NavButton}">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="All"/>
                        <Condition Binding="{Binding ActiveNavigation}" Value="All"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Recent"/>
                        <Condition Binding="{Binding ActiveNavigation}" Value="Recent"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Favorites"/>
                        <Condition Binding="{Binding ActiveNavigation}" Value="Favorites"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="AI"/>
                        <Condition Binding="{Binding ActiveNavigation}" Value="AI"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </MultiDataTrigger>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Settings"/>
                        <Condition Binding="{Binding ActiveNavigation}" Value="Settings"/>
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
    <Grid x:Name="ShellContent" Background="{DynamicResource WindowBackground}" ClipToBounds="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="212" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="68" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.RowSpan="2" Grid.ColumnSpan="2" ClipToBounds="True" IsHitTestVisible="False">
            <Image Source="{Binding BackgroundImage}" Stretch="UniformToFill" Opacity="{Binding BackgroundOpacity}">
                <Image.Effect><BlurEffect Radius="{Binding BackgroundBlur}" KernelType="Gaussian" /></Image.Effect>
            </Image>
        </Border>

        <Border Grid.RowSpan="2" Background="{Binding SidebarPanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
            <Grid Margin="14,22,14,14">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="9,0,0,16">
                    <Border Width="44" Height="44" CornerRadius="10" Background="Transparent">
                        <Image x:Name="BrandIconImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality" SnapsToDevicePixels="True" />
                    </Border>
                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="YumeShelf" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock Text="你的智能游戏盒子" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="LIBRARY" Foreground="{DynamicResource MutedTextBrush}" FontSize="10" Margin="11,0,0,5" />
                    <Button Content="▦  全部游戏" Command="{Binding NavigateCommand}" CommandParameter="All" Tag="All" Style="{StaticResource ShellNavButton}" />
                    <Button Content="◷  最近游玩" Command="{Binding NavigateCommand}" CommandParameter="Recent" Tag="Recent" Style="{StaticResource ShellNavButton}" />
                    <Button Content="♡  收藏" Command="{Binding NavigateCommand}" CommandParameter="Favorites" Tag="Favorites" Style="{StaticResource ShellNavButton}" />
                    <Button Content="⊞  添加游戏" Command="{Binding AddGameCommand}" Style="{StaticResource NavButton}" />
                    <Button Content="✦  AI 助手" Command="{Binding OpenAiAssistantCommand}" Tag="AI" Style="{StaticResource ShellNavButton}" />
                </StackPanel>

                <StackPanel Grid.Row="2" VerticalAlignment="Bottom">
                    <Border Background="{DynamicResource CardBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="7" Padding="12" Margin="0,0,0,10">
                        <StackPanel>
                            <DockPanel>
                                <TextBlock Text="游戏目录" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" />
                                <TextBlock Text="本地" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="11" DockPanel.Dock="Right" />
                            </DockPanel>
                            <TextBlock Text="游戏文件保留在原目录" Foreground="{DynamicResource MutedTextBrush}" FontSize="10" Margin="0,7,0,7"/>
                            <DockPanel>
                                <TextBlock Text="YumeShelf 数据" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" />
                                <TextBlock Text="{Binding Games.Count, StringFormat={}{0} 项}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="11" DockPanel.Dock="Right" />
                            </DockPanel>
                        </StackPanel>
                    </Border>
                    <Button Content="⚙  设置" Command="{Binding OpenSettingsCommand}" Tag="Settings" Style="{StaticResource ShellNavButton}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid x:Name="PageHost" Grid.Column="1" Grid.RowSpan="2" ClipToBounds="True">
        <Grid x:Name="LibraryPage">
            <Grid.RowDefinitions><RowDefinition Height="68"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{Binding ShellPanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="122" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="{Binding CurrentSectionTitle}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="19" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding LibrarySummary}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" Margin="0,1,0,0" />
                </StackPanel>
                <Border Grid.Column="1" MaxWidth="340" Height="38" HorizontalAlignment="Stretch" Margin="14,0,14,0" Background="{DynamicResource SearchBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="7">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="34" /><ColumnDefinition Width="*" /><ColumnDefinition Width="32" /></Grid.ColumnDefinitions>
                        <TextBlock Text="⌕" Foreground="{DynamicResource MutedTextBrush}" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="搜索游戏标题、引擎、标签或简介" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" VerticalAlignment="Center" IsHitTestVisible="False" Visibility="{Binding HasSearchText, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                        <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SearchTextBox}" ToolTip="搜索标题、引擎、标签或简介" />
                        <Button Grid.Column="2" Content="×" Command="{Binding ClearSearchCommand}" Style="{StaticResource SearchClearButton}" Visibility="{Binding HasSearchText, Converter={StaticResource BooleanToVisibilityConverter}}" ToolTip="清空搜索" />
                    </Grid>
                </Border>
                <ComboBox Grid.Column="2" ItemsSource="{Binding SortOptions}" SelectedItem="{Binding SelectedSort}" Style="{StaticResource FilterComboBox}" VerticalAlignment="Center" Height="38" AutomationProperties.Name="游戏排序" ToolTip="排序"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="{Binding DetailColumnWidth}" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{Binding ShellPanelBrush}">
                <Grid Margin="22,21,22,26">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,18">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" Width="110" ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilter}" Style="{StaticResource FilterComboBox}" />
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                            <ToggleButton AutomationProperties.Name="封面视图" Content="▦" IsChecked="{Binding IsListView, Converter={StaticResource InverseBooleanConverter}}" Style="{StaticResource ViewToggleButton}" ToolTip="封面视图" />
                            <ToggleButton AutomationProperties.Name="紧凑列表" Content="≡" IsChecked="{Binding IsListView}" Style="{StaticResource ViewToggleButton}" ToolTip="紧凑列表" />
                            <Button Content="↻" Command="{Binding RefreshLibraryCommand}" Style="{StaticResource IconButton}" Width="30" Height="28" FontSize="17" Margin="8,0,0,0" ToolTip="刷新游戏库" AutomationProperties.Name="刷新游戏库" />
                        </StackPanel>
                        <Button Grid.Column="3" Content="＋  添加游戏" Command="{Binding AddGameCommand}" Style="{StaticResource PrimaryButton}" />
                    </Grid>

                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding VisibleGames}"
                             SelectedItem="{Binding SelectedGame, Mode=TwoWay}"
                             ItemContainerStyle="{StaticResource GameGridItem}"
                             Visibility="{Binding IsListView, Converter={StaticResource InverseBooleanToVisibilityConverter}}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             Background="Transparent"
                             BorderThickness="0">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Width="178" Background="{DynamicResource CardBackground}" CornerRadius="14" ClipToBounds="True">
                                    <Grid>
                                        <Grid.RowDefinitions><RowDefinition Height="100.125" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                        <Border Grid.Row="0" Background="{DynamicResource CoverPlaceholderBrush}" CornerRadius="14,14,0,0" ClipToBounds="True">
                                            <Border.OpacityMask>
                                                <VisualBrush Stretch="Fill">
                                                    <VisualBrush.Visual>
                                                        <Border Width="178" Height="100.125" Background="Black" CornerRadius="14,14,0,0" />
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Border.OpacityMask>
                                            <Grid>
                                                <Image Source="{Binding CoverPath, Converter={StaticResource CoverImageConverter}}" Stretch="UniformToFill" />
                                                <Border Background="#36000000" />
                                                <StackPanel VerticalAlignment="Bottom" Margin="12">
                                                    <TextBlock Text="VISUAL NOVEL" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="9" />
                                                    <TextBlock Text="{Binding Title}" Foreground="White" FontSize="17" FontWeight="SemiBold" TextWrapping="Wrap" MaxHeight="43" />
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                        <StackPanel Grid.Row="1" Margin="10,9,10,10">
                                            <TextBlock Text="{Binding Title}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,4,0,0"><Run Text="{Binding Engine}" /><Run Text="  ·  " /><Run Text="{Binding ReleaseYear, TargetNullValue=未设定}" /></TextBlock>
                                        </StackPanel>
                                        <UniformGrid Grid.Row="2" Columns="2" Margin="10,0,10,10">
                                            <Button Content="编辑" Command="{Binding DataContext.EditGameCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource CardActionButton}" Margin="0,0,4,0" ToolTip="编辑游戏信息" />
                                            <Button Content="移除" Command="{Binding DataContext.RemoveGameCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource CardActionButton}" Margin="4,0,0,0" ToolTip="从库中移除记录，游戏文件不会被删除" />
                                        </UniformGrid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <ListBox Grid.Row="1"
                             ItemsSource="{Binding VisibleGames}"
                             SelectedItem="{Binding SelectedGame, Mode=TwoWay}"
                             ItemContainerStyle="{StaticResource GameListItem}"
                             Visibility="{Binding IsListView, Converter={StaticResource BooleanToVisibilityConverter}}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             Background="Transparent"
                             BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="9" Background="{DynamicResource CardBackground}" CornerRadius="6">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="98" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /><ColumnDefinition Width="152" /></Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Height="62" Background="{DynamicResource CoverPlaceholderBrush}" CornerRadius="4" ClipToBounds="True">
                                            <Grid><Image Source="{Binding CoverPath, Converter={StaticResource CoverImageConverter}}" Stretch="UniformToFill" /><Border Background="#36000000" /><TextBlock Text="{Binding Title}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" Margin="8" VerticalAlignment="Bottom" /></Grid>
                                        </Border>
                                        <StackPanel Grid.Column="1" Margin="13,0,10,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Title}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="14" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                                            <TextBlock Foreground="{DynamicResource MutedTextBrush}" FontSize="11" Margin="0,4,0,0"><Run Text="{Binding Engine}" /><Run Text="  ·  " /><Run Text="{Binding RootPath}" /></TextBlock>
                                        </StackPanel>
                                        <TextBlock Grid.Column="2" Text="{Binding LaunchStatusText}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" VerticalAlignment="Center" />
                                        <UniformGrid Grid.Column="3" Columns="2" VerticalAlignment="Center" Margin="10,0,0,0">
                                            <Button Content="编辑" Command="{Binding DataContext.EditGameCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource CardActionButton}" Margin="0,0,4,0" ToolTip="编辑游戏信息" />
                                            <Button Content="移除" Command="{Binding DataContext.RemoveGameCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" Style="{StaticResource CardActionButton}" Margin="4,0,0,0" ToolTip="从库中移除记录，游戏文件不会被删除" />
                                        </UniformGrid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <TextBlock Grid.Row="1" Text="没有找到匹配的游戏" Foreground="{DynamicResource MutedTextBrush}" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding HasFilteredGames, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                    <Border Grid.Row="2" Visibility="{Binding SimpleLayout, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Background="{DynamicResource CardBackground}" CornerRadius="6" Padding="14" Margin="0,14,0,0">
                        <StackPanel>
                            <TextBlock Text="{Binding SelectedGame.Title, TargetNullValue=选择游戏后可使用下方操作}" Foreground="{DynamicResource PrimaryTextBrush}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Margin="0,0,0,10" />
                            <WrapPanel>
                                <Button Content="▶ 启动游戏" Command="{Binding LaunchGameCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,16,6" />
                                <Button Content="收藏 / 取消收藏" Command="{Binding ToggleFavoriteCommand}" Style="{StaticResource LinkButton}" Margin="0,0,16,6" />
                                <Button Content="打开目录" Command="{Binding OpenFolderCommand}" Style="{StaticResource LinkButton}" Margin="0,0,16,6" />
                            </WrapPanel>
                        </StackPanel>
                    </Border>
                    <StackPanel Grid.Row="3" Margin="0,12,0,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,8" Visibility="{Binding HasMultiplePages, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Button Content="‹" Command="{Binding PreviousPageCommand}" Style="{StaticResource IconButton}" AutomationProperties.Name="上一页" ToolTip="上一页"/>
                            <TextBlock Text="{Binding PageSummary}" Foreground="{DynamicResource MutedTextBrush}" VerticalAlignment="Center" Margin="10,0"/>
                            <Button Content="›" Command="{Binding NextPageCommand}" Style="{StaticResource IconButton}" AutomationProperties.Name="下一页" ToolTip="下一页"/>
                        </StackPanel>
                        <TextBlock TextWrapping="Wrap" MaxHeight="44" TextTrimming="CharacterEllipsis" ToolTip="{Binding StatusMessage}" Text="{Binding StatusMessage}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="1" Visibility="{Binding FullLayout, Converter={StaticResource BooleanToVisibilityConverter}}"
                    Background="{Binding DetailPanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1,0,0,0">
                <Grid Margin="20,22">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="SELECTED GAME" Foreground="{DynamicResource MutedTextBrush}" FontSize="10" VerticalAlignment="Center" />
                        <Button Content="{Binding FavoriteGlyph}" Command="{Binding ToggleFavoriteCommand}" Style="{StaticResource IconButton}" DockPanel.Dock="Right" ToolTip="收藏" AutomationProperties.Name="收藏或取消收藏" />
                    </DockPanel>
                    <Border Grid.Row="1" Height="155.25" Background="{DynamicResource CoverPlaceholderBrush}" CornerRadius="14" ClipToBounds="True">
                        <Border.OpacityMask>
                            <VisualBrush>
                                <VisualBrush.Visual><Border Width="276" Height="155.25" Background="White" CornerRadius="14" /></VisualBrush.Visual>
                            </VisualBrush>
                        </Border.OpacityMask>
                        <Grid>
                            <Image Source="{Binding SelectedGame.CoverPath, Converter={StaticResource CoverImageConverter}}" Stretch="UniformToFill" />
                            <Border Background="#36000000" />
                        </Grid>
                    </Border>
                    <TextBlock Grid.Row="2" Text="{Binding SelectedGame.Title, TargetNullValue=未选择游戏, FallbackValue=未选择游戏}" TextWrapping="Wrap" MaxHeight="62" TextTrimming="CharacterEllipsis" ToolTip="{Binding SelectedGame.Title}" Foreground="{DynamicResource PrimaryTextBrush}" FontSize="22" FontWeight="SemiBold" Margin="0,17,0,3" />
                    <TextBlock Grid.Row="3" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" TextWrapping="Wrap"><Run Text="{Binding SelectedGame.Engine, TargetNullValue=未知引擎}" /><Run Text="  ·  " /><Run Text="{Binding SelectedGame.ReleaseLabel, Mode=OneWay, TargetNullValue=未设定}" /></TextBlock>
                    <ScrollViewer Grid.Row="4" Margin="0,14,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel>
                        <WrapPanel>
                            <Border Background="{DynamicResource Surface2Brush}" CornerRadius="4" Padding="7,4" Margin="0,0,6,6"><TextBlock Text="本地" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" /></Border>
                            <Border Background="{DynamicResource Surface2Brush}" CornerRadius="4" Padding="7,4" Margin="0,0,6,6"><TextBlock Text="{Binding SelectedGame.Availability, TargetNullValue=未选择游戏, FallbackValue=未选择游戏}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" /></Border>
                            <Border Background="{DynamicResource Surface2Brush}" CornerRadius="4" Padding="7,4" Margin="0,0,6,6"><TextBlock Text="Windows" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" /></Border>
                        </WrapPanel>
                        <TextBlock Text="{Binding SelectedGame.GameType, StringFormat=类型：{0}}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        <TextBlock Text="{Binding SelectedGame.Description, TargetNullValue=选择一个游戏查看详情。, FallbackValue=选择一个游戏查看详情。}" Foreground="{DynamicResource MutedTextBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,8,0,0" />
                        <TextBlock Text="{Binding SelectedGame.RootPath}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" TextTrimming="CharacterEllipsis" Margin="0,16,0,0" ToolTip="{Binding SelectedGame.RootPath}" />
                    </StackPanel></ScrollViewer>
                <Grid Grid.Row="5" Margin="0,20,0,0">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="44" /></Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="▶  启动游戏" Command="{Binding LaunchGameCommand}" Style="{StaticResource PrimaryButton}" />
                        <Button Grid.Column="1" Content="⋯" Command="{Binding OpenFolderCommand}" Style="{StaticResource IconButton}" Margin="8,0,0,0" ToolTip="打开游戏目录" />
                    </Grid>
                    <DockPanel Grid.Row="6" Margin="0,14,0,0">
                        <TextBlock Text="游戏操作已移至游戏条目卡片" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" />
                        <TextBlock Text="{Binding SelectedGame.LastPlayedAt, StringFormat=上次游玩：{0:yyyy-MM-dd}, TargetNullValue=上次游玩：未游玩}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11" HorizontalAlignment="Right" />
                    </DockPanel>
                </Grid>
            </Border>
        </Grid>
        </Grid>
        <local:AiAssistantPage x:Name="AssistantPage" DataContext="{Binding AiAssistant}" Visibility="Collapsed"/>
        <local:SettingsPage x:Name="SettingsContent" DataContext="{Binding SettingsEditor}" Visibility="Collapsed"/>
        </Grid>
        <local:FeedbackToast x:Name="OperationToast" Grid.Column="1" Grid.RowSpan="2" Panel.ZIndex="100"
                             DataContext="{Binding Feedback}" Margin="20"/>
    </Grid>
    <local:StartupIntro x:Name="StartupIntroduction" Visibility="Collapsed" Panel.ZIndex="200"/>
    </Grid>
</Window>
