# YumeShelf 开发日志

## 记录规则

- 每次需求、范围、技术决策、代码变更和验证结果都追加记录。
- 需求调整先更新 `docs/galgame-launcher-development.md`，再修改代码。
- 没有实际运行的构建、测试或人工验收不得标记为通过。
- 未决事项统一放回开发文档的“未决决策”章节。

## 2026-09-12：首个垂直切片

### 需求与决策

- 确认首版只支持 Windows。
- 确认采用深色 Windows 桌面工具风格：左侧导航、中间游戏库、右侧详情和启动按钮。
- 确认项目目录为 `G:\galgameStart`。
- 当前环境未检测到 `dotnet` 命令，因此先完成源码和文档落地，编译验证留到具备 .NET 8 SDK 的环境。
- 为控制首版依赖和尽早验证核心链路，当前持久化暂时使用 `System.Text.Json` + `%LOCALAPPDATA%\YumeShelf\library.json`。
- SQLite 仍是目标方案，但必须在 JSON 数据导入和回滚策略明确后再迁移。

### 已完成

- 创建 `YumeShelf.sln` 和 `src/YumeShelf/YumeShelf.csproj`。
- 创建 WPF 应用入口和深色主题资源。
- 创建 `Game` 领域模型。
- 创建 JSON 本地存储，使用临时文件替换保存。
- 创建游戏库服务和 Windows 进程启动服务。
- 创建主窗口 ViewModel：添加 `.exe`、搜索、筛选、收藏、启动、进程退出监听、最近游玩和累计时长。
- 创建主窗口界面：左侧导航、中间封面库、右侧详情、橙色主操作按钮、封面/列表视图和基础空状态。
- 增加编辑游戏信息窗口，支持修改标题、引擎、年份、工作目录、启动参数、封面路径和简介。
- 修正编辑窗口资源绑定、对话框 Owner 和 Windows 进程启动异常处理。
- 使用完全限定的 WPF `Application.Current`，避免与项目的 `YumeShelf.Application` 命名空间发生解析冲突。
- 增加 `scripts/build.ps1` 和 `scripts/run.ps1`，统一 .NET SDK 检查与构建/运行入口。
- 左侧入口名称调整为“添加游戏”，与当前只选择 `.exe` 的实现保持一致；父目录批量扫描暂未实现。
- 创建 `.gitignore`、`README.md`。
- 将开发文档同步到当前实现状态。

### 文件变更

```text
.gitignore
README.md
YumeShelf.sln
src/YumeShelf/YumeShelf.csproj
src/YumeShelf/App.xaml
src/YumeShelf/App.xaml.cs
src/YumeShelf/Common/ObservableObject.cs
src/YumeShelf/Common/RelayCommand.cs
src/YumeShelf/Common/Converters.cs
src/YumeShelf/Domain/Game.cs
src/YumeShelf/Application/GameLibraryService.cs
src/YumeShelf/Application/GameLaunchService.cs
src/YumeShelf/Infrastructure/JsonGameStore.cs
src/YumeShelf/Presentation/MainWindow.xaml
src/YumeShelf/Presentation/MainWindow.xaml.cs
src/YumeShelf/Presentation/MainWindowViewModel.cs
src/YumeShelf/Presentation/GameEditorWindow.xaml
src/YumeShelf/Presentation/GameEditorWindow.xaml.cs
src/YumeShelf/Resources/Colors.xaml
src/YumeShelf/Resources/Styles.xaml
scripts/build.ps1
scripts/run.ps1
docs/galgame-launcher-development.md
docs/development-log.md
```

### 验证

已执行：

```powershell
Get-ChildItem -Force
rg --files -g '!*\.git*' -g '!bin' -g '!obj'
dotnet --info
```

结果：

- 项目目录检查通过，原有开发文档保留。
- 源码文件已写入项目目录。
- `dotnet --info` 未执行成功：当前环境找不到 `dotnet` 命令。
- 未声称构建、测试或 WPF 运行验证通过。

追加静态检查：

```powershell
[xml](Get-Content -Raw src\YumeShelf\App.xaml)
[xml](Get-Content -Raw src\YumeShelf\Presentation\MainWindow.xaml)
[xml](Get-Content -Raw src\YumeShelf\Resources\Colors.xaml)
[xml](Get-Content -Raw src\YumeShelf\Resources\Styles.xaml)
```

结果：

- 四个 XAML/XML 文件均已通过 XML 解析。
- 检查过程中发现并修复主窗口空状态文本的 `Visibility` 绑定缺少结束引号问题。
- 14 个 C#/XAML 源文件的花括号静态计数平衡。
- 仍未进行 WPF 编译、启动和真实游戏进程验收。

### 当前风险与下一步

1. 在安装 .NET 8 SDK 的 Windows 环境中执行 `dotnet build`，修复可能出现的 WPF/XAML 编译问题。
2. 用真实 Ren'Py、Unity、Kirikiri 游戏分别验证工作目录、参数和进程退出行为。
3. 增加真正的数据库备份/恢复，而不是只依赖 JSON 文件复制。
4. 补充单元测试项目；当前未引入测试框架，避免在 SDK 不可用时伪造测试结果。
5. 确认 JSON 到 SQLite 的迁移时机和回滚策略。

### 本轮收尾验证

- PowerShell 脚本解析：`scripts/build.ps1`、`scripts/run.ps1` 均通过。
- XAML XML 解析：`App.xaml`、`MainWindow.xaml`、`GameEditorWindow.xaml`、`Colors.xaml`、`Styles.xaml` 均通过。
- C#/XAML 花括号静态检查：16 个源码文件平衡。
- 工具链检查：未找到 `dotnet`、`msbuild`、`csc`，也未发现常见 .NET/Visual Studio 安装目录。
- `scripts/build.ps1` 能正确识别缺少 .NET 8 SDK 并给出安装提示；未进行实际构建。
- 未进行 WPF 界面运行、真实游戏启动、进程监听和发布包验收。

当前状态：源码和项目结构已准备好，但还不能在当前机器直接生成 exe。安装 .NET 8 SDK 后，应先运行 `scripts\\build.ps1`，再运行 `scripts\\run.ps1`，将构建错误和人工验收结果继续追加到本日志。

### 2026-09-12：安装 SDK 并开始真实构建

- 通过 `winget` 安装 Microsoft .NET 8 SDK 8.0.425，安装成功。
- SDK 验证通过，包含 `Microsoft.WindowsDesktop.App 8.0.31`，满足 WPF 构建条件。
- 首次 `dotnet build .\\YumeShelf.sln --configuration Debug --nologo` 发现 `YumeShelf.Application` 命名空间遮蔽 WPF `Application` 类型。
- 已将 `App.xaml.cs` 的基类改为 `System.Windows.Application`，准备重新构建。
- 第二次构建发现三个文件缺少 `System.IO` 引用，以及编辑窗口年份局部变量的 definite-assignment 问题；已修正，继续构建。

### 2026-09-12：Debug 构建与应用启动验证

#### 已完成

- 确认 Microsoft .NET 8 SDK 版本为 `8.0.425`。
- 确认 WPF 桌面运行时 `Microsoft.WindowsDesktop.App 8.0.31` 已安装。
- 使用 SDK 绝对路径完成 Debug 构建：

```powershell
& 'C:\\Program Files\\dotnet\\dotnet.exe' build .\\YumeShelf.sln --configuration Debug --nologo
```

- 构建结果：0 个警告、0 个错误。
- 启动可执行文件：

```text
G:\\galgameStart\\src\\YumeShelf\\bin\\Debug\\net8.0-windows\\YumeShelf.exe
```

- 进程检查结果：PID `4432`，窗口标题为 `YumeShelf · 本地游戏库`，`Responding=True`，`HasExited=False`。

#### 环境说明

- 新开的 PowerShell 会话没有自动继承 `dotnet` PATH，因此直接执行 `dotnet` 会提示找不到命令；使用 `C:\\Program Files\\dotnet\\dotnet.exe` 后构建正常。
- 之前启动的进程在后续检查时已退出，未将其误记为持续运行；本轮已重新启动并再次确认进程状态。
- Windows UI 自动化桥接当前未提供可用的 native app 控制接口，故本轮未声称完成截图级、控件级或对话框交互验收。
- 已更新 `scripts/build.ps1` 和 `scripts/run.ps1`：当 PATH 中没有 `dotnet` 时，自动回退到 `%ProgramFiles%\\dotnet\\dotnet.exe`，避免 SDK 已安装但当前会话 PATH 未刷新时误报。

#### 当前状态

当前 Debug 版本可以在本机启动并显示主窗口。下一步应补做窗口控件交互验收，并使用无害测试程序验证添加、编辑、启动、退出监听和游玩时长记录，再进入 Release 发布和备份恢复工作。

### 2026-09-12：加入本地测试程序

#### 变更

- 新增 `tests/YumeShelf.TestGame` 控制台项目，并加入 `YumeShelf.sln`。
- 测试程序默认运行 10 秒，可通过 `--duration` 调整运行时间。
- 测试程序会将工作目录、进程 ID、启动时间和收到的额外参数写入当前目录的 `test-game-run.log`，随后自动退出。
- 设计目的：验证 YumeShelf 的 exe 路径、工作目录、启动参数、进程退出监听和游玩时长记录，不下载或修改任何真实游戏。

#### 待验证

- 需要在 YumeShelf 界面中添加生成的 `YumeShelf.TestGame.exe`，再执行一次完整的启动和退出测试。
- 本轮先完成源码和解决方案构建，未将该测试程序误记为已经通过 UI 操作验收。

#### 构建与运行验证

- `dotnet build .\\YumeShelf.sln --configuration Debug --nologo`：通过，0 个警告、0 个错误。
- 使用 `YumeShelf.TestGame.dll --duration 1 --profile test` 运行：通过，程序正常退出。
- 运行报告确认记录了当前工作目录、进程 ID、启动时间、运行秒数和额外参数。
- 测试运行在项目目录中生成了 `test-game-run.log`，该文件仅为临时验证产物；当前环境的命令执行策略阻止了后续删除操作。

### 2026-09-12：代执行启动器测试

- 使用项目自带 `YumeShelf.TestGame` 作为测试游戏，避免依赖或下载真实商业游戏。
- 测试程序实际运行 1 秒并正常退出，报告确认工作目录为 `G:\\galgameStart`，并正确收到 `--profile test` 参数。
- 使用 `scripts/build.ps1` 完成 Release 构建，结果为 0 个警告、0 个错误。
- 最终重新启动 YumeShelf Debug 版本：PID `25136`，窗口标题 `YumeShelf · 本地游戏库`，`Responding=True`，`HasExited=False`。
- 当前可确认：项目可构建、测试程序可运行并自动退出、YumeShelf 主窗口可启动。
- 当前仍未确认：通过 UI 点击“添加游戏”和“启动游戏”后由 ViewModel 记录游玩时长；UI 自动化桥接没有提供可用的 native app 控制接口，因此未将这部分写成已通过。

### 2026-09-12：用户实机完成添加与启动

- 用户通过 YumeShelf 界面成功添加本地测试程序。
- 用户通过详情面板成功启动 `YumeShelf.TestGame`。
- 截图中主窗口显示 `1 个已添加项目` 的库摘要，并可见测试条目；状态栏显示“正在运行 YumeShelf.TestGame”。
- 测试程序窗口显示启动时间、工作目录、进程 ID 和 10 秒运行倒计时，证明启动文件和工作目录配置已生效。
- 该结果补齐了“界面添加 → 界面启动 → 测试程序运行”的真实人工验收环节。
- 后续检查确认测试程序已自动退出，`library.json` 已写入 `LastPlayedAt`、`TotalPlaySeconds=10` 和 `LastLaunchStatus=Completed`。
- 至此已完成“界面添加 → 界面启动 → 进程运行 → 自动退出 → 游玩记录持久化”的完整真实测试。

### 2026-09-12：增加自定义背景图片

#### 变更

- 新增设置窗口，启用左侧“设置”入口。
- 在设置中增加“选择图片”和“清除”操作，支持 PNG、JPG、JPEG、BMP、GIF。
- 主窗口增加背景图片显示层，使用低透明度保留原有深色工具风格和文字可读性。
- 新增 `AppSettingsStore`，将背景路径保存到 `%LOCALAPPDATA%\\YumeShelf\\settings.json`。
- 启动时检查背景文件是否存在，失效路径自动回退默认背景。

#### 构建说明

- 首次构建因旧版 YumeShelf 进程锁定 Debug exe 失败；该问题属于运行中的文件占用，并非代码编译错误。
- 需要关闭旧版 YumeShelf 后重新构建并启动新版本，再进行设置窗口交互验收。

#### 验证结果

- 已关闭占用旧文件的进程并重新构建 Debug 版本：0 个警告、0 个错误。
- 已启动包含背景设置功能的新版本：PID `18396`，窗口标题为 `YumeShelf · 本地游戏库`，进程响应正常且未退出。
- 设置窗口的实际选图交互仍需在本机选择一张图片完成最终人工验收。

### 2026-09-12：修复设置窗口闪退

- 根据 Windows Application Error 和 .NET Runtime 事件定位到设置窗口崩溃原因：只读属性 `BackgroundImagePath` 的 TextBox 绑定默认使用 TwoWay，WPF 在窗口布局时抛出 `InvalidOperationException`。
- 已将该绑定明确改为 `Mode=OneWay`，保留只读路径展示和按钮写入设置的行为。
- 待重新构建并启动后，通过设置入口进行回归验证。
- 已关闭旧进程并重新构建 Debug 版本：0 个警告、0 个错误。
- 已启动修复后的版本：PID `18332`，窗口标题 `YumeShelf · 本地游戏库`，进程响应正常且未退出。
- 由于当前 UI 自动化桥接仍无法控制 native WPF 窗口，本轮已完成构建和启动回归，但未虚构点击设置按钮后的人工验收结果。

### 2026-09-12：重做分类设置页与背景效果预览

#### 需求与实现

- 根据用户反馈，将单一选图对话框重做为独立设置页：左侧“应用美化 / AI 检索 / 页面分栏”，右侧分类内容，底部固定“取消 / 确认并保存”。
- 应用美化增加图片内容预览和文件名、恢复默认、背景显示强度（0%–100%）和高斯模糊（0–30 px）。透明度仅影响图片，不使整个 Windows 窗口或文字变透明。
- 使用 WPF 原生 BlurEffect 与图片 Opacity，未引入额外图像处理依赖。图片加载完成即释放源文件句柄，预览和主窗口共用解码逻辑。
- 设置采用独立草稿；选图、清除、滑块和分栏变更不会立即写入或改变主窗口。取消、Esc、关闭窗口丢弃草稿，确认成功后才提交全部设置。
- 图片损坏、缺失和保存失败会在页面中显示提示，保留草稿供修正；保存失败不关闭窗口。
- 简化布局实际隐藏右侧详情栏，并在库底部提供启动、编辑、收藏、打开目录、移除操作；完整布局保留三栏。
- AI 检索保留可切换的分类页面并明确标注尚未接入，本轮未实现模型调用和联网检索。
- 扩展 settings.json 字段并兼容旧版图片路径配置。同步更新开发文档第 10.4 节和 README。

#### 验证与证据

- Debug 和 Release 解决方案构建均通过：0 警告、0 错误。
- 增加可重复运行的无额外依赖检查项目：tests/YumeShelf.SettingsChecks。
- 实际验证旧配置兼容、图片解码与文件句柄释放、预览不写入、取消不写入、确认保存与重读、恢复默认、损坏和缺失图片、模拟写入失败。
- 加载真实 WPF 设置控件并执行布局、绑定与离屏渲染，验证三个分类内容切换、切换分类保留草稿、效果值绑定和简化/完整布局宽度；未发现 WPF binding error。
- 检查默认和小尺寸设置页、完整与简化主页面渲染图，修复预览区边缘裁切和禁用链接按钮的系统白底。
- 最终渲染证据保存在 tests/YumeShelf.SettingsChecks/bin/Release/net8.0-windows/test-artifacts/083517e42d4842bd917ac6cc6ccf15ff/。
- 以上为真实 WPF 组件渲染及程序化回归，不是桌面鼠标操作截图；Windows 文件选择器的鼠标交互未自动化。
- 正常关闭旧版主窗口后重建并启动新版 Debug 应用，保留用户现有游戏库及已保存背景配置。

### 2026-09-13：当前版本完整回归

- 检查了游戏库数据和已有背景设置，确认现有测试游戏记录与背景路径可读取。
- 运行 `YumeShelf.SettingsChecks`：全部检查通过，覆盖旧设置兼容、图片预览、确认/取消、恢复默认、透明度、高斯模糊、分类切换、写入失败提示、完整/简化布局和 WPF 绑定。
- 解决方案 Release 构建通过：0 个警告、0 个错误。
- 生成并检查应用美化、页面分栏、完整页面和简化页面的 WPF 渲染结果，预览图不再只显示文件地址，底部确认区保持可见。
- 正常关闭旧 Debug 进程并重新启动当前 Debug 版本，启动器窗口响应正常。
- 当前仍未通过鼠标自动选择用户图片或点击真实设置窗口按钮完成桌面人工验收；Windows UI 自动化桥接不可用。程序化 WPF 渲染和隔离数据回归已覆盖对应逻辑。

### 2026-09-13：修复详情操作按钮被边界裁切

- 根据用户截图定位到详情面板操作栏的第二列只有 40px，但 `IconButton` 宽 36px 加左侧 8px 外边距实际需要 44px，导致“打开目录”按钮向右溢出并被边界裁切。
- 已将该列调整为 44px，使按钮完整落在布局列内。
- Release 构建、隔离 Debug 编译和 `YumeShelf.SettingsChecks` 回归检查均通过，均为 0 个警告、0 个错误。
- 已重启修复后的 Debug 应用：PID `20832`，窗口标题为 `YumeShelf · 本地游戏库`，响应正常且未退出。

### 2026-09-13：优化主窗口搜索框

- 将搜索框改为固定宽度的完整输入区域，增加“搜索游戏标题、引擎、标签或简介”占位提示。
- 保留输入即筛选逻辑，增加仅在有内容时显示的清空按钮；清空后恢复全部游戏。
- 增加清空命令的状态更新，避免搜索框为空时出现无效操作。
- 搜索框和清空按钮使用独立样式，保留键盘焦点、悬停反馈和深色主题。

### 2026-09-13：调整搜索框输入体验

- 搜索框改为浅色背景和浅色边框，提高与深色主界面的对比度。
- 将输入框左内边距改为 8px；搜索图标位于独立列，光标和文字从输入区域左侧开始，不再为图标重复预留左边距。
- 调整占位文字、光标、选中背景和清空按钮颜色，保证浅色输入区域中的可读性。

### 2026-09-13：美化游戏类型下拉筛选

- 将系统默认 ComboBox 替换为自定义样式：浅色圆角显示区、独立下拉箭头、焦点/悬停边框反馈。
- 展开菜单使用深色面板和圆角选项，选中项使用橙色强调，悬停项使用深灰高亮。
- 保留现有“全部类型 / 已收藏 / 最近游玩”筛选逻辑，不改变数据行为。

### 2026-09-13：提高移除游戏记录的可发现性

- 删除功能原本位于右侧详情面板底部，名称为“从库中移除”，功能正常但颜色过于接近普通次要链接，不易发现。
- 改为更明确的“移除记录”危险操作样式，并增加提示：只删除启动器中的库记录，不删除游戏文件。
- 保留原有确认弹窗，避免误操作。

### 2026-09-13：统一游戏操作面板

- 将“编辑信息”和“移除记录”收进右侧游戏操作面板，与“启动游戏”及打开目录按钮处于同一视觉容器内。
- 两个按钮改为橙色背景和白色文字，保持与“添加游戏”“启动游戏”一致的主操作风格。
- 移除功能仍只删除库记录，并保留确认弹窗和不会删除游戏文件的提示。

### 2026-09-13：将条目操作移动到游戏面板

- 根据示意图，将“编辑”和“移除”放入每个游戏条目自身：封面卡片底部显示，紧凑列表行尾显示。
- 按钮采用橙色实心白字，使用条目参数执行，点击任意条目内按钮不会依赖先手动选中其他条目。
- 右侧详情区移除重复的编辑/移除按钮，仅保留启动和游玩信息；简化页面也沿用条目内操作。

### 2026-09-13：修正页面透明度调节逻辑

- 确认旧“背景显示强度”只作用于背景图片，无法有效改变页面层次，实用性不足。
- 保留背景图片显示强度作为内部兼容字段，新增 `PageTransparency` 设置，滑块现在控制侧栏、游戏库和详情面板的透明程度；图片存在时面板会透出背景，文字、按钮和封面内容保持清晰。
- 滑块关闭刻度吸附，使用 0.01 的最小步进和 0.1 的大步进，避免只能在两个端点间切换。
- 更新设置页文案、开发文档、README 和隔离回归检查。
- Release 构建成功：0 个警告、0 个错误；`YumeShelf.SettingsChecks` 全部通过，新增验证页面透明度持久化和主窗口应用。

### 2026-09-13：当前版本功能回归

- 检查当前用户游戏库和已有背景设置，确认路径、游戏条目及游玩记录可读取。
- 执行 `YumeShelf.SettingsChecks`：全部通过，覆盖图片预览、透明度、模糊、确认/取消、布局切换、损坏图片和 WPF 绑定。
- Release 构建通过：0 个警告、0 个错误。
- 为避免覆盖仍在运行的旧 Debug exe，将当前 Debug DLL 输出到隔离运行目录并启动；窗口句柄有效、标题为 `YumeShelf · 本地游戏库`、响应正常，最近两分钟没有新的 .NET Runtime 崩溃事件。
- 旧 Debug 进程未接受正常关闭请求，未强制结束，以避免丢失可能存在的未保存界面状态；隔离运行实例用于当前版本检查。

### 2026-09-13：添加游戏支持自动本地搜索

- 添加独立的“添加游戏”窗口，默认提供自动搜索添加，也保留手动选择 EXE。
- 自动搜索支持选择磁盘或文件夹，最多递归 5 层、最多遍历 2000 个目录和展示 200 个候选；无权限目录会跳过。
- 根据 Unity、GameMaker、RPG Maker、KiriKiri、TyranoScript、NW.js 等资源特征评分，并排除安装、卸载、更新和启动器程序；扫描不会执行任何 EXE。
- 候选结果显示标题、路径、引擎和识别原因，用户确认勾选后才添加，重复路径会自动跳过。
- Release 构建通过：0 个警告、0 个错误。

### 2026-09-13：修复添加窗口点击闪退

- 从 Windows 应用程序事件日志定位到添加窗口创建阶段的 `XamlParseException`。
- 原因是窗口引用了项目不存在的 `TextButton` 静态资源；已改用现有的 `SecondaryButton` 样式。

### 2026-09-13：重新设计添加游戏窗口

- 根据界面反馈重做添加页布局：增加副标题和步骤提示，将搜索范围、扫描结果、底部状态分别放入清晰的视觉区域。
- 自定义自动搜索/手动添加模式切换样式，移除原生 TabControl 的白色默认外观；候选条目改为深色卡片并突出引擎和匹配度信息。
- Release 构建通过：0 个警告、0 个错误；应用启动后窗口响应正常。

### 2026-09-13：新增日间配色与独立夜间模式

- 应用美化增加樱粉色、浅蓝色、淡黄色和跟随壁纸配色四个日间选项。
- 黑色界面改为独立的夜间模式开关；夜间模式开启时覆盖日间配色，关闭后恢复所选日间配色。
- 跟随壁纸配色在未选择自定义壁纸时禁用，清除壁纸后会自动回退到樱粉色，避免保存不可用状态。

### 2026-09-13：修正配色覆盖范围

- 根据界面反馈，修复配色只影响强调色的问题。
- 主页面的搜索框、侧栏、顶部区域、游戏库、详情面板、筛选控件、标题和标签改用主题资源；日间配色现在整体使用白色基底，夜间模式整体使用深色基底。
### 2026-09-13：将三张视觉背景作为内置主题资源

- 将 `visual` 目录中的粉色、蓝色、暖黄色背景图打包到应用 `Assets/Backgrounds`，随 Release 输出。
- 选择樱粉色、浅蓝色或淡黄色时，主页面和设置预览会自动使用对应内置背景；夜间模式不使用日间背景。
- 主题面板、搜索框、筛选下拉菜单、详情占位图、设置预览和布局示意图统一改用动态主题资源，消除日间主题中的深色残留区域。
- Release 构建和设置回归检查均通过。

### 2026-09-13：补充 BGI 引擎识别

- 根据 `樱之诗V1.2` 实际目录结构，补充 BGI 引擎特征：`BGI.gdb`、`BGI.kdb`、`BGI.hvl` 和 `data*.arc`。
- 该目录现在可以识别 `BGI.exe`、`BGI.chs.exe` 等游戏启动文件；`Uninstaller.exe` 仍会被排除。
### 2026-09-13：修复实际使用中的添加与编辑问题

- 添加窗口继续优化空间布局相关实现，避免自动搜索和手动添加内容互相覆盖；编辑窗口增高并为简介提供可滚动的最小编辑区域。
- 扫描结果按游戏目录合并，只保留可信度最高的启动文件，并排除 BGI 的 `BHVC.exe` 辅助程序，避免同一游戏生成多个条目。
- 手动和自动添加增加“同目录 + 同引擎”去重，重复添加会被跳过。
- 游戏退出监听只更新游玩记录，不会删除游戏；保存异常时保留内存中的条目并显示错误信息。
- Release 构建和设置回归检查通过。

### 2026-09-13：调整添加方式切换按钮

- 为“自动搜索添加”和“手动添加”设置统一按钮宽度和高度，避免文字和边框显示不完整。
- 移除默认焦点虚线，增强选中态背景、边框和文字颜色的区分。

### 2026-09-13：固定添加页切换区域尺寸

- 自动搜索和手动添加内容卡片统一固定高度，切换模式时扫描结果区和底部操作栏不再上下跳动。
- 扫描结果标题与“可多选”、全选按钮保持单行排列，避免窄窗口下文字挤占和换行。

### 2026-09-13：修复扫描后的确认按钮文案

- 扫描完成后主按钮现在明确显示“确认添加”，只有没有候选结果时才显示“开始扫描”。
- 确认添加仍会依据勾选状态批量入库，未勾选的候选不会被添加。
### 2026-09-13：加强重复条目清理

- 手动添加继续按完整启动路径去重，BGI 同目录同引擎也只允许保留一条记录。
- 应用启动加载游戏库时增加历史重复数据清理，并立即保存清理后的库文件，避免旧版本重复记录再次出现。
- 游戏退出流程仍只更新状态和游玩记录，不执行移除操作。

### 2026-09-13：增加刷新游戏库按钮

- 在主页面视图切换按钮旁增加刷新按钮。
- 刷新会重新读取本地游戏库、执行重复记录清理、更新列表和筛选结果，并尽量恢复原选中游戏。
### 2026-09-13：整理 AI 助手接入方案

- 在项目根目录新增 `ai-integration-development.md`，记录 AI 助手、Galgame 自由检索、游戏资料补全和本地 HTML/Markdown 资料页需求。
- 确定首选 OpenAI 兼容 API，并预留 Ollama 本地模型适配器；搜索服务、领域过滤、结构化字段校验和用户确认写入均独立设计。
- 后续新增 AI 能力或调整服务接入方式时，需要同步更新根目录 AI 文档和本开发日志。
### 2026-09-13：增强智能启动文件识别

- 扩展自动扫描排除规则，过滤配置工具、补丁工具、运行库、崩溃处理程序和其他辅助程序。
- 根据启动文件名称、引擎特征、文件体积和同目录资源综合评分，优先推荐 `game`、`start`、`play`、`main`、汉化入口等更像游戏主入口的 EXE。
- 同一游戏目录按可信度只保留一个候选，避免把多个辅助 EXE 当成不同游戏。
- 2026-09-13：修复非标准视觉小说启动文件识别。扫描器现在会识别“EXE 与同名资源文件（如 `.bin`）配对”的自定义引擎游戏，并排除 `cfg.exe`/`cfg_chs.exe` 等配置程序；对 `廃村少女 番外 ～籠愛拾遺～` 优先识别 `haison_fd2_chs.exe`。
- 2026-09-13：增加目录级 Galgame 证据：存档目录、汉化/中文标记、日文字符和常见视觉小说资源扩展名（`.bin`、`.dat`、`.pak`、`.arc`）会参与评分；扫描结果会显示命中的识别依据。
- 2026-09-13：使用 G 盘实测并修正误报：移除单个资源文件和上级中文目录带来的弱判断，未知引擎必须具备更强 Galgame 上下文；扫描目录上限改为按已访问目录计数，避免大磁盘队列过早截断。G 盘最终识别出 3 个 Galgame，未混入 Quark、VMware、Python、手机助手、安装包等普通程序。
- 2026-09-13：加入离线游戏元数据解析。添加游戏时优先使用游戏目录名生成标题，并在游戏目录内按封面/标题/Logo 等文件名、图片比例和尺寸筛选本地图片作为封面；无法识别时保留原有 EXE 名称和空封面，不联网、不修改游戏文件。
- 2026-09-13：加入用户提供的 `DefaultCover.png` 作为离线模式默认封面。游戏目录没有合适图片或读取图片目录失败时，自动回退到项目内置封面，并随 Release/发布包复制输出。
- 2026-09-13：开始全面 Dashboard 风格改造。统一浅色主题的留白、文字层级、低对比边框、柔和阴影和强调色，并同步覆盖主页面、设置页、添加游戏页、编辑页及其按钮、输入框、下拉框和列表控件；夜间模式仍保留独立深色配色。
- 2026-09-13：修复设置窗口顶部标题栏显示褐色强调色的问题。设置窗口改用与页面背景一致的 WindowChrome 客户区标题栏，并保留系统窗口控制按钮和缩放行为。
- 2026-09-13：将无褐色强调色的 WindowChrome 标题栏同步应用到主窗口，避免主界面和设置窗口出现不同的系统标题栏风格。
- 2026-09-13：修复 WindowChrome 导致主窗口和设置窗口的最小化、最大化、关闭按钮消失的问题。恢复原生标题栏按钮，并通过 DWM 将标题栏背景和文字颜色同步到当前页面主题。
- 2026-09-13：根据新的视觉方向移除设置页默认配色选择器，日间界面统一为白底、黑字、灰色阴影和分隔线，夜间模式保留独立黑灰主题；主页面游戏封面卡片改为更圆润的 14px 圆角，并调整封面区域接近 16:9 比例。
- 2026-09-13：修复游戏封面图片未真正裁剪圆角的问题。为封面区域增加圆角不透明蒙版，确保未选中、悬停和选中状态都不会露出直角图片边缘。
- 2026-09-13：按界面反馈调整操作区：明确左下目录状态卡和右下详情快捷操作容器的用途，移除右下独立白色背景；导航、排序、刷新、收藏、视图切换和更多按钮统一使用白底，悬停时通过灰色阴影强调。
- 2026-09-13：修复游戏卡片选中/悬停时外层出现多余白色圆角的问题，改为透明状态背景并仅保留边框和阴影；删除右侧详情封面内重复的游戏名称叠加层。
- 2026-09-14：开始接入 AI 第一阶段。新增 OpenAI 兼容 API 连接测试、地址/模型/超时配置和 DPAPI 加密 Key 存储；设置页 AI 检索栏已替换为真实配置界面。自由检索和资料补全暂未开启。
- 2026-09-14：推进 AI 第二阶段，新增 AI 助手窗口、Galgame 领域关键词过滤、OpenAI 兼容 Chat Completions 调用、取消请求和错误显示；主导航增加 AI 助手入口。当前回答功能使用已配置的云端服务，网页来源检索和资料写回仍待后续实现。
- 2026-09-14：修复 AI 助手被领域过滤拦截时状态仍显示“就绪”的交互问题；空问题、无关问题和未配置 Key 现在都会给出明确状态提示。
- 2026-09-14：根据 DeepSeek-Harness 的 agent loop/session/provider 思路重构 AI 助手基础。移除本地关键词硬拦截，改由模型根据角色提示和会话历史判断问题范围；加入最多 12 条消息的多轮上下文，并明确当前能力、工具边界和安全限制。已发现的 DSH 源码位于 `G:\deepseek-harness-master\deepseek-harness-master`，暂不直接嵌入其 Electron 运行时。
- 2026-09-14：采用用户提供的“游戏文件夹 + 作品卡片 + 手柄”图标，复制为项目内置 `YumeShelfIcon.png`，并应用到主窗口和设置窗口；图标随 Release 输出。
- 2026-09-14：将主窗口左侧品牌区的旧菱形占位标识替换为正式 YumeShelf 图标，使窗口图标、设置窗口图标和应用内部品牌标识保持一致。
- 2026-09-14：修复品牌图标显示为空白的问题。主窗口品牌图标改为从 Release 发布目录加载与窗口图标相同的 PNG，避免 XAML 相对路径在 Content 资源模式下解析失败。

### 2026-09-14：确认 Yume 专用智能体开发基线 v1.0

- 用户确认采用讨论后的 Yume 方案，后续按该方案分阶段开发，实际测试发现问题后再调整。
- 全面重写根目录 `ai-integration-development.md`：统一角色、服务范围、七类语义路由、检索与证据整合、输出模板、回答自由度、token/工具/时间预算、上下文与任务生命周期。
- 删除旧文档中本地关键词过滤和“无关问题不调用模型”的现行要求；明确无关问题仍需一次短路由请求，但不进入检索或长回答。历史日志中的关键词方案仅代表当时实现。
- 补充独立对话工作页布局、来源卡、历史、重试、逐字段审核、写入冲突及撤销、本地 HTML/Markdown 资料页和分阶段验收要求。
- 对照现有 Provider、助手 ViewModel/XAML、设置与密钥存储核对实现状态：当前仍为简单问答；共享取消源竞争、重复发送、Key 明文输入控件及仅检查 `/models` 等列为后续待办，不标记为已修复。
- 明确此前仅参考 DSH 的设计思路，没有复用核心代码或完成许可证/依赖审查；首版优先沿用现有 .NET 架构，不引入完整 Electron/Node 运行时。
- 同步总开发文档的 AI 基线入口和 README 的当前能力说明；保留原有历史记录。
- 本次仅更新文档：检查章节、路由示例、链接目标及旧规则残留；未修改应用代码、调用真实 API、构建或重启应用，不将文档检查描述为功能测试。

### 2026-09-15：开始 S1 可靠专用对话改造

- 将 AI 助手从“只显示最后一次回答”改为可滚动的完整会话消息区，保留用户消息与 Yume 回复。
- 增加模型语义路由：先以短 JSON 请求判断 intro、out_of_scope、clarify、answer、research、metadata 或 document；无关问题使用固定短拒绝，不进入长回答；当前没有检索工具时，相关长任务仍明确说明未接入联网检索。
- 发送中禁止重复提交，每次请求使用独立取消源；关闭窗口会取消任务；旧任务不会清理新任务的取消源。
- 失败或停止时保留原输入，只有完整成功回合进入会话历史，避免下一次请求携带失败残留。
- Provider 增加可选输出上限、温度和响应校验；路由不强制发送 `response_format`，提高不同 OpenAI 兼容服务的兼容性。
- 设置页 API Key 改为 PasswordBox；将“测试连接”改为“检查服务”，新增“测试模型回答”，区分 `/models` 可用与所填模型能够实际推理。
- 修正设置回归检查中过时的 AI 文案断言；Release 构建和 `YumeShelf.SettingsChecks` 均通过，0 警告、0 错误。
- 本次尚未接入真实网页检索、来源卡、字段审核、资料页和本地历史；未执行真实 API 调用，模型语义路由仍需用模拟服务和固定样本集验证。

### 2026-09-15：设置与 AI 融入主窗口，增加上下过渡导航

- 根据用户三张示意图，将 SettingsWindow / AiAssistantWindow 转为 SettingsPage / AiAssistantPage（WPF UserControl），由主窗口右侧 PageHost 展示；左侧主导航固定，切页不再创建这两个独立窗口。
- 游戏库搜索栏和详情随游戏库整页切换；当前导航以固定空间边框与加粗文本强调，避免边框出现时把相邻按钮向下推移。
- 按导航上下顺序加入 220ms / 36 DIP 缓出滑动及淡入淡出，遵守系统关闭客户端动画的偏好；快速导航以最新目标为准，离场页禁用交互，防止动画回调覆盖新页。
- 保留游戏库搜索/视图/选中项、设置草稿以及 AI 输入/会话；设置确认或取消均留在页内，成功后显示状态，取消恢复已保存配置。设置主题预览仍局限于设置页，保存后更新主窗口与标题栏。
- AI 页切出不取消当前请求，主窗口关闭取消 AI 和设置测试请求；保存模型配置只影响下一轮请求，同一轮的路由和回答使用配置快照。
- 调整窄屏设置配色卡、测试按钮及状态文本，AI 发送/停止按钮固定在右下方；长输入限制高度并内部滚动，对话支持选择复制。
- 不引入 WebView、前端框架或外部导航依赖；同步根目录 AI 文档 v1.1、总开发文档和 README。
- 修正测试宿主：只加载生产资源，不启动真实应用的 StartupUri；JsonGameStore 增加可选测试路径，回归使用隔离游戏库与设置。测试样例数据和图片均生成于测试目录。
- 隔离 Release 构建 0 警告、0 错误；原设置回归及新增页面导航回归通过。验证 1200×720 / 900×580 内容区、长输入、确认/取消、主题、上下动画方向、60 次连续切页后只剩一个可交互页面、导航固定、无子窗口和 WPF 绑定错误。
- 已查看设置宽屏、设置窄屏、AI 日间/夜间的 WPF 渲染图；未调用真实 API，未执行多显示器 DPI 实机验收。联网检索、Markdown、资料审核和跨重启会话历史不在本次页面改造中实现。
- 正式 Release 构建与完整回归复跑通过（0 警告、0 错误），包含手动下拉筛选保留检查。正常关闭旧 Release 后已启动新版，主窗口已创建并响应；未强制终止旧进程。正式回归渲染图保存在 `tests/YumeShelf.SettingsChecks/bin/Release/net8.0-windows/test-artifacts/47e44c019e3e408491aaac72c2f6d402/`。

### 2026-09-15：统一操作反馈与保存失败恢复

- 针对设置确认后反馈不明显的问题，新增共享 OperationFeedback 与 WPF FeedbackToast，主窗口右上角跨页显示，添加窗口使用局部提示。沿用现有依赖与主题，圆角卡片区分成功、普通、警告、错误和处理中状态。
- 成功/普通提示 4 秒后关闭，重复结果重新呈现并计时；悬停和键盘焦点进入时暂停计时。错误和警告保留供查看，可手动关闭；支持可选恢复操作按钮。
- 覆盖设置确认/取消、背景选择/默认预览、API 检查/测试、扫描、批量添加、重复添加、编辑、收藏、移除、刷新、启动、退出记录和打开目录。AI 请求失败/停止/超时保留输入并反馈，正常回答本身作为成功结果。
- 修复写入失败后仍被后续成功文案覆盖的问题。游戏库保存返回真实结果；新增/移除先持久化再更新列表；编辑和运行记录写入失败保留内存修改，提供重新保存，阻止刷新丢失修改；损坏数据刷新失败时保留原列表。
- 添加结果区分成功/重复/失败并汇总数量，部分失败留在添加窗口供重试。扫描期间禁用重复提交/更换路径，修改搜索范围清除旧候选；固定底部按钮宽度，长状态文字自动换行。
- 设置页底部同步显示未保存/保存失败/保存成功，避免保留上次成功文案；取消旧设置测试后禁止迟到结果覆盖新提示。API 错误用安全摘要，模型测试不直接展示模型返回文本。
- 增加隔离反馈回归，验证真实临时文件写入成功及受阻、重复提示、保存失败草稿保留、重复/部分添加、保存重试、刷新保护、缺失启动文件、缺 Key、深浅主题、窄窗口及计时。所有数据使用测试目录，无真实 API 调用或真实游戏启动。
- 初次隔离 Release 构建和完整回归通过，0 警告/错误、无 WPF 绑定错误；已查看宽/窄设置、保存重试、部分添加及夜间提示渲染图。随后根据渲染发现的底部旧状态补充修复与断言，最终验证记录见下。
- 同步总开发文档 §10.6、AI 开发文档 v1.2 §9.4 和 README；该迭代不改变 AI 路由/检索能力的完成状态。
- 最终正式 Release 构建通过（0 警告、0 错误），完整设置/导航/反馈回归通过且无 WPF 绑定错误；再次查看窄窗口保存失败截图，确认底部不再残留成功状态。正式渲染证据位于 `tests/YumeShelf.SettingsChecks/bin/Release/net8.0-windows/test-artifacts/5234d05d2bfb46c6b07852b964d32be9/`。
- 正常关闭旧 Release 进程 17772 后启动新版进程 22180，确认运行路径为正式 Release，主窗口标题为 `YumeShelf · 本地游戏库`、窗口句柄已创建且保持响应。未强制终止应用，未修改用户游戏作为测试样本。

### 2026-09-15：全面审查数据安全、交互与代码质量

- 按用户要求完成当前项目审查，新增根目录 `project-audit-2026-09-15.md`，记录 20 个主要审查项、证据、优先级、修复建议及工程质量问题。本轮未修改业务代码或宣称问题已修复。
- 使用 `.runtime/project-audit/probes/` 中的独立 WPF/HTTP/存储探针，21 个针对性场景复现：损坏库覆盖、缺字段/null 启动异常、两个写入实例覆盖、运行中刷新丢失记录、单次添加视图重复、扫描漏报/误报/深度截断、过度去重、损坏封面、布局溢出、夜间硬编码颜色、默认预览不一致以及 AI 协议/预算/重试问题。
- 游戏运行检查只调用自带 TestGame（2 秒后正常退出）；导入用伪 EXE 不执行。API 检查仅在回环地址使用模拟服务与虚构 Key。未操作真实游戏文件、用户游戏库或真实 API 凭据。
- 默认隔离 Release 构建及已有设置/导航/反馈回归通过，无编译警告/错误和 WPF 绑定错误。扩展分析强制重编译后发现 CA1001（未释放 HttpClient）、CA1031（宽泛捕获异常）、CA1869（序列化选项反复创建）等告警，已与默认构建结果区分。
- NuGet 当前源未报告已知漏洞包；framework-dependent 发布成功，必要图片资源齐全。实际验证 `dotnet test` 只执行 VSTest 构建目标，没有运行控制台回归，列为门禁缺口。
- 最终探针证据：`.runtime/project-audit/probe-build/bin/AuditProbes/release/evidence/5f9e0cc9d717489382891a509213f4c1/`，包含结果 JSON、布局截图和无害进程日志。探针通过表示复现了问题，修复后须改为断言正确行为。
- 同步 README、总开发文档及 AI 文档的审查入口。原 Release 进程 22180 保持运行，本轮没有重启或替换用户当前应用。

### 2026-09-15：全面修复 R01–R20，版本 0.2.0

- 按用户“针对问题全面修复”的要求，落实审查报告全部 20 项；审查报告 §9 逐项记录行为、证据和未完成的发行验收。沿用 .NET/WPF 原生能力，未新增第三方依赖。
- 先保存完整项目快照 `.runtime/repair-baseline-20260915/`，随后建立本地 Git，以 Codex 作者记录修复前源码快照 `4c68f97`。忽略运行数据/日志/构建产物，不推送远程仓库，不变更用户全局 Git 身份配置。
- 游戏库增加稳定 ID/记录校验、损坏只读状态、显式恢复原件留存、16 MiB 预算、写入锁、哈希冲突检查、刷盘临时文件及上一版备份。启动不再自动清空或去重写回库。
- 修复集合通知中重入 Refresh 的重复卡片；运行中刷新按 ID 更新当前对象并记录退出；移除仍运行的条目不会在退出后重建。遗留 Running 标记为记录中断。未保存的失败修改退出前可重试/放弃/取消，失败重试不关闭。
- 自动/手动共享引擎检测，支持 Ren'Py 目录及自定义脚本/背景/语音 BIN 结构，通用引擎必须有额外上下文。实际目录复测发现无假名/无存档的廃村少女漏检，已补足资源组合判据和同名 EXE/BIN 配对，排除配置程序、优先汉化入口。
- 只读扫描樱之诗V1.2 和 Gamex-006800，分别找到 `BGI.chs.exe` 与 `haison_fd2_chs.exe`；本轮未启动第三方游戏，不将两目录结果视为全 G 盘或所有引擎验收。
- 扫描明确返回不完整原因，统一 12 层/10000 目录/500 候选/4096 枚举项/60 秒软上限，跳过重解析和无权限目录。候选勾选可通知 UI，批量元数据读取移至工作线程，批量只提交一次库文件。
- 统一图片校验/缩略图/默认封面回退，单图 32 MiB/4000 万像素、128 候选/2 秒软预算。专项回归发现 InvalidDataException 不属于 IOException 的捕获缺口，补齐图片与库边界，避免超大文件触发崩溃。
- 编辑页可重新绑定启动 EXE，保留游戏 ID、收藏及统计；长表单/简介可滚动、底部保存固定。主窗口初始化导航延后至继承数据上下文完成，解决部分 TextBlock 初始绑定中断而简介/状态空白的问题。
- 卡片封面 16:9，详情真正圆角裁剪；夜间统一控件资源，分离按钮底色与强调文字色，关键文字对比度 4.5:1。排序、中文运行状态、真实可启动状态、键盘焦点、可访问名称与 AI Enter/Shift+Enter 完善。
- 500 条目首次加载/渲染/筛选约 4.5 秒，改为每页 60 条后同场景 1344 毫秒；全库搜索/排序、翻页后的选中项仍有效。未引入自定义虚拟化面板。
- AI 云端 HTTPS/本机 HTTP 回环边界，禁止自动重定向，变更来源清空 Key；共享 HttpClient、整任务 5–120 秒期限、请求/响应/历史/澄清预算。原消息重试、取消和截断状态明确，失败/无关内容不污染成功历史。测试只使用虚构 Key 和回环模拟 API。
- 增加脱敏轮换日志、启动/全局异常处理；清理无调用 AiDomainGuard、StrongFiles 和旧配色选项，密钥存储归入 Infrastructure；复用序列化配置，增加编辑器/换行规范。
- 使用原有用户图标生成 16/32/48/256 多尺寸 ICO，内嵌 EXE 与版本 0.2.0。统一 `scripts/check.ps1` 串联带警告门禁的 Release 构建、两套控制台回归和依赖框架发布资源/版本/图标检查；修复 build/run 脚本退出码，日志捕获原生命令输出。
- 最终统一检查通过：0 编译警告/错误，SettingsChecks 全部通过，ReliabilityChecks 92 项（含两个可选真实目录）通过，无 WPF 绑定错误，发布校验通过。完整日志 `.runtime/checks/check-20260915-203626.log`；可靠性截图/JSON 位于 `.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/acd530972cf34b42b27935039edee792/`，设置证据位于 `.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/53cf469cd5d14ed7bf1470270f20979a/`。
- 同步主开发文档 v0.2、AI 文档 v1.3、README 与审查报告。真实模型语义、联网检索/资料审核、物理输入法/读屏器/跨 DPI、干净 Windows 安装升级、自包含发行和更大压力场景仍明确标为待验收。
- 收尾增加未选中游戏的明确提示与中性封面占位，避免残留蓝底和空白说明；额外读取全规则分析结果，处理原生标题栏 HRESULT 记录，并对已在 OnExit 释放的 WPF 单实例 Mutex 作局部生命周期说明，其余设计/命名建议未宣称全部消除。
- 收尾后的最终统一门禁再次通过：SettingsChecks 全部通过、ReliabilityChecks **93 项**（默认不附加目录为 91 项）通过，0 编译警告/错误，无 WPF 绑定错误，发布校验通过。新证据目录：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/cee2af3000964b0a99795986941e5714/`；设置渲染：`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/fa46e1620dc7444f849bdcae28eaca47/`。500 条目最后一次测量 1550 毫秒，较改进前约 4.5 秒降低。
- 正式 Release 构建通过；启动进程 23136，确认窗口标题、原生句柄、响应状态和版本 0.2.0.0。第二次启动的进程正常退出并激活已有窗口，保持单实例。启动前后用户 library.json/settings.json 的 SHA-256 均一致；没有清理或重写用户数据。

### 2026-09-15：扩展游戏扫描与引擎识别

- 按用户确认的方案保留“视觉小说优先”默认模式，新增“扩展游戏扫描”，让具备游戏资源的 ACT/SLG/RPG 等作品不再必须带有日文、汉化或存档特征；引擎识别不自动推断玩法，也不扩大 Yume 的 Galgame 服务范围。
- RPG Maker 细分 2000/2003、XP、VX、VX Ace、MV、MZ；支持常见 RGSS 包与散装数据、MV/MZ 顶层及 www 结构。补充 WOLF RPG、NScripter/ONScripter、RealLive、Siglus、Artemis、YU-RIS；Artemis/YPF 与 GameMaker 检查短文件头。
- 自动扫描和手动入库继续共享识别函数。BGI 去掉单独 data*.arc 的宽泛判据；单个 KS 不再冒充 TyranoScript。Unity 要求 EXE 对应的 _Data 游戏资源，保留同目录各自配对的独立入口，排除未配对辅助程序；纯 NW.js 只标注框架，不单凭框架入选。
- 共用资源的已知引擎选择排序靠前的入口，保留汉化优先；配置、卸载、编辑器以及伪装成 EXE 的目录不进入候选。跳过开发缓存/版本库/系统回收站子目录，避免项目里的合成测试样例影响整盘扫描。资源头读取限制、资源被占用等问题明确报告扫描不完整。
- 视觉小说入口显示“推荐入口”且预选；扩展与未知引擎候选显示“待确认”且不预选，保留依据，核对勾选后才入库。未知引擎兜底不再标成已确认的自定义视觉小说。内部排序分不再显示为容易误解的匹配度。
- 添加窗口内新增主题下拉选择、重新扫描、空状态和可换行的识别依据；路径省略并提供完整提示。两种添加方式表单保持等高，扫描结果独立滚动，确认按钮固定。目录/模式改变清空旧候选与反馈；扫描和导入期间冻结模式、勾选与重复提交。
- 新增 ScanCoverageChecks：31 个典型入口样例及普通运行库、编辑器、残缺/伪资源等反例；涵盖模式隔离、手动标签与重复保护、上下文兜底、选中后确认、重新扫描、窄窗口、等高面板、夜间主题、文件头预算和资源锁定。首轮发现窄窗口结果区偏矮，调整表单高度后复验通过。
- 最终统一门禁 `.\scripts\check.ps1 -ScanRoots ...` 通过：SettingsChecks 全部通过、ReliabilityChecks **144 项**（不附加真实目录为 142 项），0 编译警告/错误、无 WPF 绑定错误，依赖框架发布、图标和版本资源校验通过。两处真实目录仍识别到 `BGI.chs.exe` 与 `haison_fd2_chs.exe`，只读扫描未启动第三方游戏；AI 回归仅使用回环模拟服务。
- 证据日志：`.runtime/checks/check-20260915-213615.log`；扫描/界面/可靠性证据：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/732a4b9d998f4921ae45c7be58b10ffb/`；原设置回归证据：`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/531739856f984fc18b7832b9fd37e771/`。已查看日间、夜间和窄窗口渲染。
- 同步总开发文档 v0.3、README，并新增 `docs/game-scanning.md` 记录判据、版本边界与验证依据；沿用 .NET/WPF 与现有服务，未新增第三方依赖。结构测试不等于全引擎/全市场兼容，其他引擎、特殊导出方式和更广泛真实样本仍待补充。
- 正常关闭旧应用后完成正式 Release 构建并启动进程 4512，确认主窗口、运行路径与响应状态正常。重启前后真实 library.json/settings.json 的 SHA-256 一致。本轮功能改动保存在本地，未执行新的远程推送。

### 2026-09-15：Yume 正文流式输出与中断恢复

- 按用户提出的长时间等待后整篇突然出现的问题，将正式回答切换为 `stream: true` 的 Chat Completions/SSE 接收。保留现有短 JSON 领域路由和固定无关拒答；不展示路由 JSON 或 reasoning_content，不改变角色、服务范围及请求预算。
- 复用现有 HttpClient、请求构造、HTTPS/回环限制、重定向禁止和脱敏错误处理，新增有界 UTF-8/SSE 解析。支持网络分包、中文/emoji、BOM、LF/CRLF/CR、多行 data、心跳、角色空片段及 usage-only 事件；累计响应 1 MiB，单事件 128×1024 字符，正文 12000 字符。
- 首段正文立即显示，后续由 UI 线程计时器约每 50ms 合并更新同一气泡，任务退出时刷新缓冲并停止计时器。位于底部时跟随新回答；阅读历史时保持滚动位置，忽略内部文本框冒泡滚动，避免被拉回末尾。
- 停止、断线、超时、错误事件和异常完成原因保留原问题与已显示正文并标记未完成；长度截断保留有界正文。只有完整成功结果进入后续模型历史，重试复用气泡并重新生成，不混入旧请求残留。路由截断不把内部 JSON 误显示为正式回复。
- 对忽略 stream 参数但返回 application/json 的兼容服务，使用同次响应并明确标注“服务返回非流式回答”；不自动再发请求、不自动重连或续写。流式改善正文生成过程的可见性，路由、排队和模型开始输出正文前仍有等待。
- 扩展本机模拟 HTTP 服务，加入真实分段/chunked 响应、单字节切分与释放闸门。新增 28 项流式检查，验证“服务暂缓后半段时 UI 已呈现首段”，以及取消/重试隔离、总期限、协议错误、正文/事件/累计字节上限、历史过滤、JSON 兼容及 WPF 日夜/滚动呈现。测试仅使用虚构 Key，不调用用户配置的真实 API。
- 最终统一门禁通过：SettingsChecks 全通过、ReliabilityChecks **172 项**（含两个真实目录；默认 170 项），0 编译警告/错误，无 WPF 绑定错误，依赖框架发布、图标和版本资源校验通过。先前扩展扫描及真实游戏入口回归保持通过。
- 证据日志：`.runtime/checks/check-20260915-220603.log`；流式/可靠性证据：`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/fd60aecbcbed48f0be783aa579dea719/`；设置回归证据：`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/7eeffef197c246b69e388156d6e42da8/`。已查看首段生成中、完成及滚动呈现截图；第三方真实服务延迟和兼容性仍待实际使用验证。
- 同步 AI 文档 v1.4、总开发文档 v0.4、README 和本日志，引用实际查阅的官方流式协议说明。沿用 .NET 8/WPF，未新增第三方依赖。
- 正常关闭旧进程 4512，正式 Release 构建通过后启动进程 28680，确认主窗口及响应状态正常；重启前后 library.json/settings.json 的 SHA-256 一致。保留上轮未提交的扫描改动，本轮没有推送 GitHub。

### 2026-09-15：Yume 看图识别游戏

- AI 助手增加选择图片、粘贴截图、输入框 Ctrl+V、缩略预览和移除；每条消息一张，附图后支持不填写文字直接识别。准备/发送时锁定重复操作，失败、超时、停止保留问题、附件和已接收正文，重试复用原气泡；同样问题但替换图片按新任务处理，避免串图。
- 设置新增可选 AiVisionModel，留空沿用 AiModel，与原有 API 地址及 DPAPI Key 共用。旧配置默认兼容且不在启动时重写；保存后的配置从下一任务生效。现有“测试模型回答”明确只验证文字，识图要使用支持图片输入的模型，不擅自替换用户模型或试用真实 Key。
- 复用 OpenAiCompatibleProvider 的请求构造、连接池、预算、错误处理和 SSE；含图消息按实际核对的官方协议发送 text/image_url 内容数组，纯文字仍为字符串。范围判断与正式回答均使用本次视觉模型和图片，相关任务最多两次模型调用；无关图片短拒答，疑似游戏但不知作品名时允许分析画面线索。
- 识别提示要求“结果/候选 → 画面依据 → 可靠时简短介绍 → 必要时补充线索”，最多三候选、不编概率或来源，不把截图里的他人回答当事实；图片内指令不能改变领域边界。当前没有反向搜图、联网来源卡、自动下载封面或写入游戏信息，未宣称已联网查证。
- 图片后台准备：源文件 32 MiB/4000 万像素限制，长边最多 1600 像素，必要时缩至 1200/900，使 PNG 不超过 4 MiB。复制像素重新编码以移除元数据、保留透明度；不传文件路径/名称。仅待发送附件持有优化后的图片数据，成功后释放；气泡仅保留 256 像素缩略图，后续模型历史仅保留相关成功文字与历史未重发图片说明。数据只在本次运行的内存中保留，不写日志/设置/游戏库。
- 窄窗口首次复验发现附件挤占对话区域，缩小留白及预览高度后修复；发送栏固定，当前截图、已发缩略图、长文本分别适配现有主题和滚动。额外处理 WPF TextBox 对纯位图剪贴板禁用原生粘贴命令的情况，在 Ctrl+V 进入原生命令前接管图片，普通文字仍保持原生行为。
- 新增 AiVisionChecks 32 项，使用合成图片、虚构 Key 和本机模拟服务，覆盖图片比例/像素/字节/元数据、预览不上传、图文协议与模型选择、流式首段、历史不重发旧图、异常/停止/换图/重试、剪贴板路由事件、旧设置兼容及日夜/窄窗口渲染。没有调用真实 API，真实模型识别准确率、视觉语义约束及物理剪贴板/跨 DPI 尚未验证。
- 整体回归中处理既有反馈计时测试的桌面干扰：透明测试窗口仍可能命中用户鼠标而暂停倒计时，现将该隐藏测试窗口设为不参与命中，并检查无悬停/焦点后验证计时；应用的悬停暂停行为保持原逻辑。
- 同步 AI 文档 v1.5、总开发文档 v0.5、README 和本日志，按 ponytail 技能复用现有 .NET/WPF 流程，没有新增 SDK、运行时依赖或本地模型；按 openai-docs 技能核对官方 Chat Completions 图片消息格式。
- 最终统一门禁通过：SettingsChecks 全部通过、ReliabilityChecks **204 项**（含两个只读真实目录；默认 202 项），0 编译警告/错误、无 WPF 绑定错误，依赖框架发布及图标/版本资源校验通过。此前扫描规则、流式恢复与 TestGame 启动统计回归保持通过。
- 最终日志 `.runtime/checks/check-20260915-223547.log`；图片/流式/可靠性证据 `.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/96e5b783af25493f87a08ff80c012911/`；设置证据 `.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/dda6d91c6d6c493cb9e5acfafcc9953c/`。已查看优化后的窄窗口、正确背景的夜间、识图流式生成中及设置页渲染。
- 正常关闭旧进程 28680，正式 Release 构建通过后启动进程 24960，确认主窗口标题、句柄与响应状态正常；启动前后真实 library.json/settings.json 的 SHA-256 一致。未重写用户库、API 配置或原图片，保留已有未提交的扫描/流式改动，本轮未推送 GitHub。

### 2026-09-16：修正添加方式按钮边缘裁切

- 实际打开“添加游戏”窗口复现“自动搜索添加 / 手动添加”右侧边缘缺失。原 TabItem 外边距与 WPF TabPanel 的布局计算冲突，最小宽度触发布局裁切；改为 TabItem 零外边距，将 8 DIP 间距放进现有模板内部，从布局源头消除右侧裁切。
- 两个按钮采用完整 8 DIP 圆角边框，局部键盘焦点样式对应按钮内部可见区域；保留原生 TabControl 的选择、方向键导航和扫描中禁用逻辑，表单高度及既有扫描/添加流程不变。仅改当前窗口 XAML，未新增依赖或自定义控件；同步总开发文档 §10.3。
- 统一检查 `scripts/check.ps1` 通过：Release 构建零警告/错误，SettingsChecks 全通过，ReliabilityChecks 默认 **202 项**通过，无 WPF 绑定错误，发布资源校验通过。本轮不重复扫描真实游戏目录。日志 `.runtime/checks/check-20260916-160807.log`；日间/夜间及自动/手动小窗口渲染证据 `.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/5c0d48f80c8c4a8d84612a7481584bdc/`。
- 通过 computer-use 在实际窗口复现旧版缺边，正常退出后构建并启动新版（PID 25520），验证鼠标切换、悬停、Shift+Tab 焦点与方向键返回自动搜索。两个按钮的四侧边框、圆角、虚线焦点框完整，底部按钮同步切换。应用已留在添加游戏页供查看；测试未扫描/添加/移除真实条目，重启前后 library.json/settings.json 的 SHA-256 均一致，未推送 GitHub。

### 2026-09-16：确认 Yume 开发期技能/工具扩展与发布版固定能力

- 用户确认后续采用“技能定义 + 工具模块 + 有限任务调度”：开发者在项目内维护 Markdown/JSON 技能与显式注册的 C# 工具，模型建议下一步，应用校验领域、能力、参数、次数和期限后执行。技能提示词不等于已经实现工具，未知工具和任意命令不能被执行。
- 发布版将审核技能嵌入程序集、工具随程序编译；不提供插件安装、技能/角色编辑、外部 DLL/脚本或目录自动加载，也不保留能经配置、环境变量或命令行恢复的开发覆盖入口。API 地址、Key、模型等正常产品设置保留；能力更新通过应用版本交付。限制针对产品正常使用，不宣称本机程序不可反编译或篡改。
- 模型调度按已验证能力选择，优先一次视觉分析提取范围、线索和候选，再按需检索或生成草稿；工具内部模型调用计入统一预算，禁止无限续轮。补充独立视觉/工具协议检测、能力记录、阶段耗时和分层超时目标，保留资料写入审核。
- 补记诊断事实：此前只读检查的微信剪贴板同时有正常 PNG 和 alpha 全为 0 的 WPF 位图，现有读取路径存在透明度兼容问题；该样本尺寸不同于用户先前截图，不声称已核验此前实际上传像素。当前带图任务已使用 AiVisionModel，两次请求共用用户配置的 30 秒整任务期限；通用超时信息不能确定网络、排队或生成的具体原因。以上问题仍待修复，没有以新增框架代替输入链路修复。
- 升级顺序明确为 S1a 图片/请求链路修复与诊断 → S1b 技能及有限调度 → S3 检索 → S4/S5 资料审核、资料页和发布验证；界面完善随对应功能推进。优先复用 .NET/WPF，DSH 仅供设计参考，第三方代码/技能需适配和审查后才可纳入。
- 同步根目录 AI 开发文档 v1.6、总开发文档 v0.6 和 README。新增技能/工具、能力匹配、预算、透明图片与发布版不可外部扩展的验收项；明确区分已确认方案、当前代码事实和未完成项目。
- 本次仅更新开发文档与日志，未修改应用代码、重启应用、调用真实 API 或推送仓库；不将既有测试记录当作新功能通过依据。验证范围为文档版本、章节引用、方案/现状一致性及变更格式检查。

### 2026-09-16：Yume 固定技能、有限调度、检索与游戏库融合升级

- 按用户已确认方案实现 YumeAgent 与 role/vision/library/research 四种 v1 技能，作为程序集资源嵌入。五个 C# 工具为 library_search、game_details、search_vndb、propose_metadata、create_document；严格 JSON 指令驱动有限循环，6 次模型/4 次工具/2 次检索上限，拒绝未知工具、多余参数、重复 JSON 键与重复调用。Debug/Release 均不提供外置技能覆盖或动态 DLL/脚本加载；开发阶段改源码后重新编译。
- 统一三种粘贴入口，优先 PNG/image/png，修复微信原生 DIB 的全零 alpha；保留合法 PNG 透明度和外部流位置，全透明/损坏/超大输入提前拒绝。识图仅首次模型请求带图并提取线索，后续对话模型复用文字，避免重复图片用量。
- 分离单次等待（默认60秒、5–120）与整任务期限（默认180秒、5–600）；SSE 数据事件刷新等待但不重置绝对期限。旧 AiTimeoutSeconds 留作服务/文字测试，启动不改原配置。增加视觉数字图和无副作用 JSON 协议测试，按服务/凭据/模型/适配身份记录内存状态；当前仅作诊断，不宣称已完成能力驱动的自动模型选择。
- AI 页新增显式关联游戏、默认关闭的库摘要许可及联网开关、来源卡、逐字段审核、文档预览与会话内单次撤销。库摘要不含路径、启动参数和统计；切换许可或关联清模型历史/草稿，模型配置变化隔离新旧历史，活动任务禁重入。
- VNDB 使用固定公开 API、无额外 Key、禁 cookie/重定向、每次最多五项、响应256KiB/20秒上限，来源URL仅由合法条目ID构造。公开查询 CLANNAD 返回 v4 / CLANNAD / 2004-04-28；没有发送用户库或模型 Key。当前不支持通用网页或反向搜图，来源不代表每个字段已自动核实。
- 资料建议默认不勾选，校验目标ID、完整元数据指纹、只读/未保存状态后复用统一库存储；先持久化副本再改原对象，失败不污染活动对象/磁盘。运行中保存与退出统计兼容，撤销保留新增游玩时间；人工变更/删除目标阻止旧草稿覆盖或复活条目。支持标题、引擎、年份、简介、标签，不改启动路径或统计。
- 文档仅先生成草稿，用户另行选择 .md/.html 导出；HTML 全文转义并限制 CSP，禁止脚本和远程资源，覆盖使用原生保存确认。收尾补齐 InvalidDataException 导出格式错误捕获和设置测试 IOException 捕获，修正设置页“尚未接入”过时文案。
- 布局回归发现窄窗口对话区偏低，压缩页头/输入留白后通过；审核、来源和选项显式使用主题资源。检查日间、小窗口及夜间审核截图；夜间测试补上真实 Window 宿主，避免脱离窗口的白底渲染造成错误结论。真实窗口使用 computer-use 检查关联、粘贴/移除、设置分类与滚动，895×703/58.2KB 微信截图预览正常，未点击发送或真实模型测试。
- 最终统一门禁 `.\scripts\check.ps1` 通过：Release 零警告/零错误，SettingsChecks 全通过、ReliabilityChecks **240项**通过（原默认202项，本轮新增38项），无WPF绑定错误，依赖框架发布/图标/版本检查通过。回归使用合成图、虚构Key、回环HTTP、隔离库和无害TestGame，没有改真实库或调用用户API。
- 最终日志 `.runtime/checks/check-20260916-182623.log`；可靠性证据 `.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/b90fb3663924435eb0f2856b8de43564/`；设置证据 `.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/6e4e5e912679460bb8ab13764ce4421f/`。已核对发布DLL含四个技能资源。正式Release另行构建并启动PID23732，窗口正常响应。
- 前后真实 library.json SHA-256 均为 `3A5EF38B6A22F699299FD931B6EAB300E398CA898429CECAC5084C608F61ADD6`；settings.json 均为 `891DA293D267EA2EF46A7E188923AFCAB11097BC69A318A62FCA7824B4992066`。保留用户配置、系统剪贴板和已有未提交改动，未提交或推送GitHub。
- 同步 AI 文档v1.7、总文档v0.7、README，新增根目录 `yume-upgrade-audit-2026-09-16.md`，记录漏洞/bug、库融合场景、代码质量与后续优先级。按ponytail技能复用标准库与现有服务，未嵌入DSH或增加运行时依赖。
- 后续优先真实供应商的视觉/领域40条样本验收、逐字段证据、详情资料页及索引，然后持久会话/Markdown/更多检索源。真实模型准确率、能力自动适配、无SDK安装环境、跨DPI和物理输入法仍未验收，不将本轮模拟通过等同于正式发行完成。

### 2026-09-16：Yume 封面查找、审核更换、撤销与修改后重启约定

- 补齐用户实测缺失的封面协作：第六工具 find_cover(query) 在关联目标和联网许可下检索 VNDB，返回最多五条候选后立即结束本轮；不让模型编造图URL或自动写库。library/research 技能升v2，角色/视觉仍v1；正文加入真实能力清单，未实现操作短答，连续无执行进展的路由提前停止。
- AI页新增可折叠“游戏封面 · 查找与更换”，可直接查询而无需模型Key或模型用量；关联后填入去掉末尾版本号的标题，可改日文/英文查询。显示来源与作品、点击下载缩略图、新旧图对照、确认更换/放弃/撤销；预览后定位确认按钮，内容随会话滚动，输入栏固定。
- 下载仅限固定VNDB图片CDN和图片路径，禁重定向/cookie/凭据转发；限定普通封面评级、MIME/魔数、缩略图2MiB/原图8MiB/每次20秒及取消。复用现有图片解码、4000万像素限制、1600长边和最多4MiB PNG重编码；不增加依赖或新的智能体运行时。
- 搜索/预览仅内存，确认才把原图优化PNG保存到应用数据Covers目录，文件名唯一。下载前后校验目标与完整资料/封面指纹，先库存储成功再改活动Game对象；失败保留原封面并仅清理本次创建且未提交的文件。封面字段不发模型，启动路径/参数/统计和游戏目录均不改动。
- 会话内撤销恢复旧路径，保留新增统计；后续人工修改/目标移除阻止覆盖。旧图和已提交新图暂不自动删除，避免影响手选图片和library.json.bak，缓存回收及持久撤销后置。
- 实测公开作品“樱之诗”成功取得VNDB v562封面：缩略/原图均解码成功，原图801×560，重编码860169字节。证据 `.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/d5ee4ba493ae43178890f9960318e03b/`；没有发送用户库、模型凭据或调用真实模型。
- 最终门禁 `.runtime/checks/check-20260916-193538.log` 通过：Release零警告/错误、设置全通过、可靠性297项通过、无WPF绑定错误、发布资源校验通过。覆盖无Key直查、模型封面调度、权限、预览不写盘、确认持久、下载/保存失败、无长度头大小限制、撤销、冲突/删除与运行中更新。可靠性及日夜/窄窗口渲染证据 `b3b2a67d3efc436080e7e6c840e9f437/`，设置证据 `6e52f4484926403c916985c1fc514ec9/`（均位于各检查程序的test-artifacts）。
- 用户要求“此后每次应用修改完成都重启”，已写入根目录AGENTS.md及开发文档。正常关闭PID11432，正式Release重建后启动PID14344，确认主窗口、新封面入口和原3条游戏仍在；恢复原游戏关联，用户同时操作窗口后停止UI操作。旧未发送问题单独保存在 `.runtime/restart-ai-draft-20260916.txt`，未发送或调用用户模型。
- 本轮真实library/settings SHA-256前后一致，具体值见审查报告§8；未改用户游戏数据、API配置、联网许可或系统剪贴板。同步AI文档v1.8、总文档v0.8、README及审查报告，未提交/推送GitHub。供应商首轮60秒超时的具体原因尚未证实，直接封面入口可绕过模型等待，不将新增工具宣称为供应商超时修复。

## 2026-09-16：Yume 对话式游戏库管家与统一任务结果

- 根据用户反馈，移除手动关联、库访问/联网勾选、粘贴图片按钮及独立封面区。默认提供所需库摘要并按任务查询VNDB，主库选中项仅作上下文；用户点名优先，模型可查库和读取真实ID，歧义需澄清。仅保留一个选择图片按钮及Ctrl+V，确认控件随结果出现。
- 新增prepare_game_update，一次取得文字与封面；单来源自动准备，多来源选择版本，字段/封面同一张修改卡一次确认。新增发行日期/游戏类型字段，兼容旧JSON并接入普通编辑器和搜索。未知引擎/类型/标签不编造，简介保留VNDB原文。
- 统一资料与封面提交，保留活动Game对象；失败完整回滚，撤销恢复最近一次整张卡片修改但保留游玩统计，删除/冲突/只读/待保存状态阻止旧草稿写入。实际保存或撤销结果进入短历史，避免模型误认尚未保存或已被撤销的资料。
- 新增scan_games：仅允许用户本轮提供的本机文件夹/EXE路径，复用视觉小说优先/扩展扫描和离线元数据。禁止扩大父目录或使用虚构/UNC路径，返回候选后确认入库，沿用原去重和持久化，不执行游戏、不下载资源包。
- 修复日期只读属性WPF绑定的默认TwoWay异常；预览超时降级为保留文字资料，允许仅保存文字；公共操作错误提示适用于资料和入库；调整新增日期区边距；新对话清除上一轮隐式游戏目标，保留未发送输入。复用.NET/现有服务，无新增依赖。
- 完整门禁 `.runtime/checks/check-20260916-212159.log` 通过：Release零警告/错误、设置/导航/反馈检查全过、可靠性329项、无绑定错误、发布资源检查通过。可靠性/渲染证据 `99b2d2dec4f549c9a31e4d3035e5c16c/`，设置证据 `82c00a03043d4a52ba0f546d6bdcd775/`（对应检查项目test-artifacts目录）。
- 真实公开VNDB复合查询“樱之诗”返回v562、简介、日期和封面；缩略/原图解码成功，801×560、860169字节，证据 `02ad13fdfb2a415dbdc99298707ceadf/public-cover-source.json`。未使用真实模型Key或改写用户库；模型语义和供应商延迟未验证。
- 正式bin单独Release重建，正常关闭PID22620后启动最终PID1264，桌面检查3条原游戏和简化AI页均正常。真实library/settings哈希前后一致，见审查报告§9；未改系统剪贴板。同步AI文档v1.9、总文档v0.9、README和审查报告，不提交/推送。后续优先真实模型评估与更多可靠资料源，下载后置。

## 2026-09-16：修复 Yume 编造本地游戏库数量与名单

- 原问题“我现在的库里有几个游戏”曾用38.9秒、2次模型/0工具生成5个不存在的作品，真实库为3条。本地库授权与回调已有，缺口在于路由允许跳过工具、正文提示丢失总数、前5候选没有注明全量计数。
- 新增AiLibraryQuery和library_query（九工具/library@4）：明确数量/列表整句在无附件时直接读取当前快照，无Key/无网络/0模型；条件筛选和分页由模型提出受限参数，程序生成结果后结束。复用已有标题匹配，按已记录引擎/类型/标签/年份统计，不推断玩法，每页20项。
- 两阶段提示补齐available/totalCount；library_search分别报告总量/匹配/返回数。answer必须声明general，否则一次纠正，再不调用工具则停止并保留输入。读库错误或未保存修改不能假装空库。复杂意图错标general仍需真实模型验收，不宣称绝对消除幻觉。
- 完整门禁 `.runtime/checks/check-20260916-220540.log` 通过：Release零警告/错误、设置全过、可靠性345项（新增16项）、无绑定错误、发布资源检查通过。原问题模拟实测2毫秒/0请求；覆盖3→7→0新快照、无Key、不可用库、筛选/分页、跳过工具和参数造假。证据目录见审查报告§10。
- 同步AI文档v1.10、总开发文档v0.10、README与审查报告。按ponytail技能复用现有.NET/服务和回归项目，不增加框架或运行时依赖；computer-use用于重启及实际窗口验证。测试未调用用户真实模型，未提交/推送GitHub。
- 正式bin另行Release构建零警告/错误，旧PID1264在确认无输入草稿/活动任务后正常关闭，新PID26004于22:16:42启动且响应正常，控件树仍显示原3条游戏。窗口自动化因前台活动返回无法激活，关闭使用进程正常关闭请求，未强杀；桌面其他应用正在使用，停止后续UI输入，未做真实窗口发送测试。真实库与配置重启前后哈希一致，具体值和验证边界见审查报告§10。

## 2026-09-17：Yume 与应用契合度审查（未实施修复）

- 新增根目录 `yume-app-integration-audit-2026-09-17.md`，审查界面/导航、库读写、自然语言调度、多轮任务、应用上下文及资料页衔接。结论为基础数据与界面融合较好，复杂任务可靠性尚未达到成熟管家水平；不以单一完成度百分比代替验收。
- 当日完整门禁 `.runtime/checks/check-20260917-180343.log` 通过，Release零警告/错误、设置全过、可靠性345项、无绑定错误及发布资源检查通过。
- 在 `.runtime/yume-fit-audit-20260917/` 增加一次性诊断，复用已有模拟服务，确认六项问题：general声明绕过事实查询、普通追问清除待确认修改、详情已读取但本地回答仍被拦截、组合任务首步结束即报完成、无法清空可选资料字段、含空格路径的较短前缀被错误接受。REPRO断言通过代表复现问题，未当作修复完成。
- 分别列出已有保护和后续能力缺口：收藏/游玩/缺失状态查询、更多资料源、绑定游戏的资料页索引、持久会话及旧入口代码清理。建议未自动升级为已确认开发需求，AI文档v1.10基线保持不变。
- 本轮仅审查、输出报告和隔离诊断；未改产品/正式回归源码、用户数据/API配置，未消耗真实模型额度、提交或推送。按ponytail技能优先检查现有业务链路，没有引入新产品依赖；没有应用实现变更，因此未重启正在使用的应用。

## 2026-09-17：修补 Yume 六项契合度缺陷并复审

- 按用户授权处理F1–F6：general自报绕过本地事实、普通追问丢待确认任务、真实详情误拦、组合请求首步即报完成、可选字段无法清空、含空格路径的较短前缀被接受。新增正式AiFitChecks，不把此前REPRO诊断通过当作修复依据。
- 生产游戏库回调接入独立语义任务核对，只看原问题和近期历史，不继承首路由建议；严格任务类型协议，温度0/输出上限300，计入原6模型/4工具预算。明确数量/列表仍0模型；一般单工具任务2次、知识回答3次。library_query与game_details依据本地快照程序输出，缺少证据时纠正一次后停止。真实语义仍依赖模型，未声称绝对消除幻觉或降低复杂任务耗时。
- 组合任务记录已完成类型并累积结果；后续失败/取消/预算耗尽保留已取得内容、原问题并显示部分完成。普通追问保留修改勾选、来源、封面预览、文档和扫描选择；保存/撤销仅处理对应任务。每类一份待确认结果，同类新结果不得静默替换，文档新增明确放弃入口。未引入通用任务队列、框架或运行时。
- 可选字段允许空字符串且显示“（清空此项）”，缺字段不等于清空，标题/非法字段/纯空白值拒绝；隔离库验证清空后的保存、重新加载及整次撤销。扫描按完整引号/裸路径规范化精确比较，保留本机/存在性/UNC限制，拒绝猜测前缀和嵌套盘符。
- library技能升v5，仍4技能/9工具。同步AI开发文档v1.11、总开发文档v0.11、README及原契合度报告的“修复与复审”；保留初审原证据，明确多目标同类任务、收藏/游玩查询、多源/资料页/持久历史及旧兼容入口的后续边界。ponytail用于复用现有业务边界和回归机制，computer-use用于正式窗口验证。
- 最终完整门禁`.runtime/checks/check-20260917-193601.log`通过：Release零警告/错误，SettingsChecks全部通过，ReliabilityChecks **382项**（比初审345项增加37项）全过，无WPF绑定错误，发布资源/版本/图标检查通过。覆盖真实SSE、字段清空/撤销、错误路由/核对、部分任务失败/取消、旧草稿冲突及完整路径边界；未使用真实模型额度。
- 可靠性证据`.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/2ec360c176684712a861cd737d342703/`；设置证据`.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/6c2aa8485779443a998b2efdfb5d8682/`。人工查看小窗口确认区、夜间审核卡的实际渲染。
- 正式bin另行Release构建通过；启动前进程/桌面均无旧YumeShelf实例，未强杀任何进程。新版PID **22172**正常响应，实际UI发送“我现在的库里有几个游戏”返回**3个、模型0次、工具1次、42毫秒**，已留在AI页。UIA设值接口缓存属性错误后改用观察到的输入区点击/键盘完成，未修改真实API配置。
- 真实library.json与settings.json前后SHA-256均一致，值见复审报告；隔离回归及实际计数未改用户游戏记录或配置。未提交/推送GitHub，保留其他工作区修改。更多资料源、跨DPI/发行环境及真实供应商语义验收仍未完成。

## 2026-09-20：YumeShelf 正式命名、YS 图标与启动介绍

- 按用户本轮需求将应用正式名称定为 YumeShelf，内置智能体仍称 Yume；统一窗口标题、侧栏品牌文案与程序集描述，不更改数据目录或迁移用户库。同步 README、总开发文档 v0.12、AI 文档品牌约定，并新增 `docs/branding.md`。
- 设计深灰 Y＋S 圆头连字、浅白圆角底与细灰轮廓，保留可编辑 SVG；原生 WPF 脚本生成 512px PNG、九尺寸 ICO 和 `docs/assets/yumeshelf-brand-layout.png` 版式。接入可执行文件、标题栏、侧栏和启动页，原默认游戏封面不变。按 ponytail 技能复用现有 WPF，无新运行时依赖。
- 新增主窗口内启动介绍：图标淡入，随后显示 YumeShelf，再显示“一个更懂你的游戏盒子...”，约 2.3 秒后进入游戏库。按钮、Enter、Esc 可跳过；遵循系统动画开关，日夜主题适配。介绍期间禁用底层交互，首帧透明避免闪烁，关闭时撤销动画；仅主实例启动播放，切页不重播。介绍在既有本地初始化完成后呈现，不宣称后台加载提速。
- 完整门禁 `.runtime/checks/check-20260920-162507.log` 通过：Release 零警告/错误、设置检查通过、可靠性 **394 项**通过（新增 12 项），无 WPF 绑定错误，发布资源/版本/图标检查通过。回归覆盖顺序、首帧、禁用底层、自动结束、三种跳过方式、减少动画、关闭生命周期、切页和隔离库保持；人工检查日间小窗口、夜间及发布图标渲染。
- 可靠性证据 `.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/3c16973685674fd4aa35f2a23f415dd1/`；设置证据 `.runtime/checks/bin/YumeShelf.SettingsChecks/release/test-artifacts/3659c16148134976ba2f3b045f2fb335/`。测试使用隔离数据，未消耗真实模型额度。
- 正式 `src/YumeShelf/bin/Release/net8.0-windows/YumeShelf.exe` 另行 Release 构建成功；启动前无旧实例，新版 PID **26468** 正常响应。computer-use 观察到介绍期间底层控件禁用，随后介绍消失、导航恢复、原 3 条游戏仍在；实际主窗口截图确认新 YS 图标与品牌名称。首次截图被其他窗口遮挡，激活请求因检测到用户输入未执行；重新只读观察已取得正常主窗口，未继续发送 UI 输入干扰用户。
- 真实 library.json 前后 SHA-256 均为 `3A5EF38B6A22F699299FD931B6EAB300E398CA898429CECAC5084C608F61ADD6`；settings.json 均为 `BA9FC41D0A82B7F45B4055687908D10EDF51C6EA79D7C4C6E40BF5C6CBF4DDC5`。未改真实游戏库/API 配置、未提交或推送，保留已有未提交修改。Windows 全部图标缓存及完整发行环境不在本轮验证结论内。

## 2026-09-22：图标小尺寸可读性与开发文档追溯

- 用户指出标题栏、侧栏图标过小，并要求文档面向人阅读、能够复盘完整开发过程。9月20日读取相关代码后用户中断，当时未实施修改；本条记录9月22日继续完成的结果。
- 原因：侧栏仅30×30且字形内部留白较多；主窗口直接使用512px PNG生成原生图标，未利用ICO的小尺寸帧。侧栏改为44×44并调整底部留白，YS路径增大、笔画由18加粗至22；16/20/24/32px原生图标进一步收紧留白。主窗口改用多帧ICO，页面/启动页保持高分辨率PNG，ICO随构建和发布复制。Windows标题栏槽位本身保持系统尺寸，未自制窗口边框。
- 同步[品牌规范](branding.md)与设计版式、[总开发文档](galgame-launcher-development.md) v0.13、[README文档入口](../README.md#记录)及[协作约定](../AGENTS.md)。明确专项文档描述当前状态，日志追加需求、原因、实现、证据和遗留项，审查报告保留历史与复审，区分计划/实现/验证。未调整AI逻辑或其开发基线。采用ponytail技能复用原生资源生成和WPF能力，无新增依赖。
- 完整门禁 `.runtime/checks/check-20260922-220620.log` 通过：Release零警告/错误，设置检查通过，原有394项可靠性回归全部通过，无WPF绑定错误，发布检查通过。视觉样式改动未新增重复实现的测试；人工查看900px隔离主界面、更新后的品牌版式及正式窗口，图标和品牌文字没有重叠或裁切。可靠性证据 `.runtime/checks/bin/YumeShelf.ReliabilityChecks/release/test-artifacts/8ede22b2a9b444f18e9a1581f8ce155e/`。
- 正式Release另行构建成功，启动前无旧实例，新版PID **29636**正常响应。computer-use确认标题栏新图标、44px侧栏图标、名称与原3条游戏正常呈现。真实library/settings前后SHA-256与上条记录一致，未调用真实模型或改写用户数据，未提交/推送。跨DPI及Windows历史快捷方式图标缓存仍未全面验收。

## 2026-09-22：GitHub 仓库迁移

- 用户更换GitHub账号并新建空仓库，要求迁移此前上传的全部Git内容。旧仓库为 `dooauror630-ship-it/YumeShelf`，新仓库为 [luyu275573-cpu/YumeShelf](https://github.com/luyu275573-cpu/YumeShelf)。通过GitHub CLI核实当前账号和目标写入权限后执行迁移，未删除旧仓库。
- 旧远端仅有 `master` 分支、2次提交，无标签；API核对无Release、议题或PR，Git历史无LFS文件。远程克隆遇到连接重置后，使用与旧远端SHA一致的本地历史建立独立裸仓库 `.runtime/repository-migration-20260922.git`，通过完整性检查后原样推送分支/标签，不使用强制或镜像删除选项。
- 新仓库默认分支为master；API确认两次提交分别为 `a84148d03e7441d1333d9a4e0e1aee91334de245`、`4c68f97ab1988be835f81efe431910be2ee27a08`。新旧分支SHA一致，树差异检查为空，原作者信息和提交历史保持。
- 本地旧origin重命名为 `legacy-origin` 供追溯；新origin指向新仓库，master跟踪 `origin/master`，默认推送目标设为origin。本仓库GitHub凭据助手使用当前已登录的GitHub CLI账号，不修改全局Git身份或保存明文令牌。
- 本轮迁移只包含旧仓库已上传内容；此前AI升级、品牌图标等大量本地未提交修改，以及本次README/日志更新仍保留在工作区，未混入历史迁移、未自动提交。README已更新当前仓库入口。仅Git配置与文档变化，未修改应用运行代码或用户数据，未重新构建/重启应用。

## 2026-09-24：清理旧测试产物，保留最近3次完整门禁

- 用户发现项目超过3GB，授权仅保留最近几次产物。本次按3次执行：保留 `check-20260917-193601.log`、`check-20260920-162507.log`、`check-20260922-220620.log` 及各自设置/可靠性证据，共6个证据目录；另保留checks当前构建、publish、仓库迁移副本和runtime根目录中的草稿文件。
- 永久删除命令被执行环境策略拒绝，未执行；改用可恢复的Windows回收站操作。移出较旧证据、24份旧门禁日志及15个旧runtime子目录，包括repair-check、fit-fix-build系列、历史诊断/布局检查、ssefilepc和ssefilepc-user临时工具目录。未清空回收站，可恢复；磁盘实际空闲空间需在回收站清空后才增加。
- 清理后重新枚举确认：`.runtime` 约284.5MiB（原3592.8MiB），整个项目约319.8MiB，项目目录减少约3.23GiB。最新3次设置与可靠性证据及日志均在，源码、Git历史、正式应用构建及用户游戏库不在清理目标内。
- README增加证据保留说明；旧开发日志与审查报告不重写，历史证据链接可能因本次清理失效，应结合本记录判断可用性。仅本次手动清理，未实现自动轮转。无产品代码变动，未重跑门禁或重启应用，避免重新生成无必要产物；未提交/推送。

## 2026-09-24：YumeTestV1、首版体验补全与测试包工程

- 用户批准按真实验收→管家体验→封包→新仓库同步推进，先交付根目录[YumeTestV1](../YumeTestV1.md)。包含54项编号用例、模型/版本批次表、前置备份、预期结果、P0/P1/P2判据、首字/总耗时和缺陷记录；真实供应商测试由用户执行，开发者不使用其真实Key或模型额度。
- library技能升v6，仍4技能/9工具；library_query增加favorite、played、state，与文字条件取交集。本地快照补充JsonIgnore字段用于判断，不把路径和原始游玩统计加入模型摘要；missing包括文件不存在或不可访问，played只表示本应用有记录，不虚构游戏实际状态。新增严格布尔类型/状态枚举校验与组合筛选回归。
- 新增AiSessionStore及会话分部类：DPAPI当前用户加密、JSON有界校验、临时文件替换保存；最多40条/16万字符文字与输入草稿，同配置恢复最多6轮/18000字符上下文。图片、来源卡、工具草稿、确认与撤销不持久化；存在活动/待确认任务时不恢复模型上下文。损坏会话保留原文件并提示，显式新对话可重建；存储失败通过统一反馈呈现。回归覆盖恢复、加密、配置隔离、清空、损坏和权限失败。
- 版本推进为0.3.0-preview.1；新增自包含Windows x64打包脚本、文件清单/SHA-256、无用户数据的`--verify-package`诊断入口及按用户目录安装脚本。安装先校验清单、按版本复制并建快捷方式，不覆盖同版本目录、不删除用户数据。发行说明明确无签名安装向导/自动更新，干净系统兼容性待验收。复用现有.NET/WPF，未增加AI框架。
- 首轮`.runtime/checks/check-20260924-180725.log`的411项可靠性通过，发布脚本因PowerShell5默认编码读取中文csproj失败。改为XML按文件声明加载后，最终门禁`.runtime/checks/check-20260924-181019.log`完整通过：零警告/错误、SettingsChecks全过、可靠性 **411项**（新增17项）、无绑定错误、发布资源/版本/图标通过。可靠性证据`962ea6f595174c2e8e17fa6b5f7b5d7c`，设置证据`6fadef6d7cd3404e91d7bf095f31a7ad`（对应检查程序test-artifacts目录）。
- 正式bin另行Release构建，启动前无旧实例，新版PID **11712**响应正常。computer-use确认原3条游戏和AI页的会话保留说明；其他应用Docker报错弹窗遮挡部分画面，未操作该弹窗，不把其错误归于YumeShelf。真实library/settings哈希与上轮一致，未发送真实模型请求。
- README、总开发文档v0.14、AI文档v1.12及发行说明同步。自包含打包首次被NuGet超时/SSL中断阻塞，正在改用微软dotnet-public公开源；仅网络失败不标为程序功能失败，封包与推送完成情况另行追加实测记录。

### 同日补充：自包含封包、隔离安装与仓库同步完成

- 官方NuGet直连恢复后取得.NETCore、WindowsDesktop和AspNetCore的8.0.31运行时包，使用本地依赖缓存完成自包含发布；未改为依赖系统.NET的包冒充自包含。可用`scripts/package.ps1 -RestoreSource <本地NuGet包目录或源地址>`切换还原源，不改变构建内容。
- 交付包：`.runtime/releases/20260924-182627/YumeShelf-0.3.0-preview.1-win-x64.zip`，75,655,794字节（约72.2MiB），SHA-256为`A0BF784FE5AFD51908BA73875819BA9EFA9DACA7E52B8E98DE345E2B6D396E5B`。清单记录源提交`60aae2381abd7188191f82cccb86b0255678c318`、`sourceDirty=false`及472个文件；源码提交已包含此前本地AI、品牌等累计实现，不只本轮新增功能。
- 发布目录`package-smoke.json`确认资源图片/图标可解码、样式可加载、4技能/9工具在包内、运行时自包含且未访问用户数据。随后从ZIP解压到独立验证目录，运行包内Install.ps1，逐一校验安装后472项哈希、快捷方式目标、重复同版本安装拒绝覆盖，再运行安装目录中的EXE诊断入口，全部通过。证据：`.runtime/package-validation-20260924-1830/verification.log`与`installed-smoke.json`；未向用户桌面创建测试快捷方式。
- 以上是当前开发机上的隔离目录验证，不能替代干净Win10/Win11、全部DPI、签名安装向导或真实用户跨版本升级验收。真实模型的54项场景仍由用户按[YumeTestV1](../YumeTestV1.md)记录结果；不将模拟回归和包诊断记为真实AI通过。正式应用PID11712保持响应，本次后续仅有封包/文档操作，无需再中断用户测试。
- 已将实现提交`60aae23`非强制推送至新仓库[luyu275573-cpu/YumeShelf](https://github.com/luyu275573-cpu/YumeShelf)，`git ls-remote`核对远端master一致。本段及发行验证说明作为后续纯文档提交同步；包清单继续指向实际构建源码提交，不重写包的来源记录。构建、依赖缓存、测试证据与真实用户数据均未纳入Git。
